# Feature Ledger

This ledger is the preservation record for Dragon MUGEN. When engine/app code changes, update this file or `docs/REGRESSION_CHECKLIST.md` in the same commit so prior work does not disappear silently.

## Preservation Rule

- New features must not remove previous features without an explicit note here.
- If a feature is intentionally replaced, document the replacement and the reason.
- If a feature is incomplete, document its current working behavior and known gap.
- The active-change guard enforces documentation updates for engine/app code commits.

## Verification Status Model

Use these labels so preservation claims do not get confused with live proof:

- `[VERIFIED-LIVE]` - Manually verified in the running app during a recorded pass or supported by a clearly recorded recent live observation.
- `[VERIFIED-STATIC]` - Confirmed by code inspection, static audit, parser test, or load-level audit, but not manually played live.
- `[CLAIMED]` - Documented or believed to exist, but not recently verified.
- `[PARTIAL]` - Works in a limited case, but not complete.
- `[REGRESSED]` - Previously worked but is currently broken.
- `[BLOCKED]` - Cannot verify yet because another system is missing, broken, inaccessible, or the verification environment cannot exercise it.
- `[NOT-STARTED]` - Planned but not implemented.

Static audits do not count as live verification. Parsed M.U.G.E.N controllers remain `[CLAIMED]` or `[VERIFIED-STATIC]` until exercised in the running game.

## Architecture Recovery Progress

| Item | Current Value |
| --- | ---: |
| Starting `App.cpp` count | 16820 |
| Current `App.cpp` count | 8510 |
| Total removed | 8310 |
| Reduction | 49.4% |
| Remaining to 50% reduction | 100 |

Latest pass: The `CtrlSet` / command-control controller runtime audit preserved the `App.cpp` `8510` checkpoint and classified `CtrlSet` before any source movement. The live body writes `fighter.ctrl` behind parsed or simple triggers and custom `firedStateCtrlControllerIds` history. Because `fighter.ctrl` gates command-state eligibility, fallback walk/jump behavior, CPU input choice, helper control, and round-finish control locks, the audit recommends command-control verifier hardening before any `CtrlSet` body move. No source, CMake, content, sidecar policy, gameplay behavior, branch topology, or `.dragon/*.json` changed.

## Current Runtime Features To Preserve

| Status | Area | Current Behavior | Preserve Until |
| --- | --- | --- | --- |
| `[VERIFIED-STATIC]` | M.U.G.E.N folder layout | Runtime content lives under `game/chars`, `game/data`, `game/font`, `game/sound`, `game/stages`, `game/plugins`, and `game/save`. | A deliberate roadmap update changes the content model. |
| `[VERIFIED-STATIC]` | M.U.G.E.N customization preservation policy | M.U.G.E.N customization preservation policy is documented in `docs/REPOSITORY_POLICY.md`, `docs/DRAGON_EXTENSIONS.md`, and `docs/STRICT_ROADMAP.md`. This means the policy is repo-visible; it does not mean full runtime enforcement has been audited. Runtime support may still be partial. The policy preserves editability and source-of-truth ownership, not instant perfect compatibility with every M.U.G.E.N character. | Always, for M.U.G.E.N-style customization. |
| `[VERIFIED-STATIC]` | Roster loading | Characters and stages are selected through `game/data/select.def`; character folders alone do not make a character selectable. | Always, for M.U.G.E.N compatibility. |
| `[VERIFIED-STATIC]` | Character file resolution | Selected character DEF `[Files]` resolves CMD, CNS/ST, `stcommon`, AIR, SFF, SND, and ACT files. | Always, for compatibility-era loading. |
| `[PARTIAL]` | Select-screen loading | Character select uses portraits/icons only and must not load full fight runtime data before the VS path. Live pass observed select portraits/icons; full memory behavior remains static-only. | A documented memory-model change replaces it. |
| `[VERIFIED-LIVE]` | VS loading path | Stage confirmation enters VS first; selected character and stage runtime loading happens on the VS-to-fight path. Live pass observed KFM/Mountainside Temple stage select, VS screen, and fight view load. | A documented flow change replaces it. |
| `[VERIFIED-LIVE]` | Modes | Training, Single Player, and VS Mode are distinct mode paths, not one menu with hidden options. Live evidence now covers Training route, Single Player pause/fight presentation, and VS/local Player 2 fight presentation. VS Mode now has simultaneous P1 and P2 Character Select cursors before Stage Select, confirmed by user-supplied screenshots after `ee1b4ae`. Individual pause actions beyond Resume still need separate checks. | A documented mode-system change replaces it. |
| `[PARTIAL]` | VS independent P2 character selection | VS Mode now stores an independent `selectedP2Character` cursor and `sessionSlots.opponentCharacter`. Character Select shows P1 and P2 cursors at the same time, tracks separate P1/P2 confirm flags, and advances to Stage Select only after both players confirm. P1 uses the normal arrows/Enter path; P2 uses the P2 keyboard/gamepad path. Character Select displays P2's selected portrait on the right panel; the VS/loading screen displays the chosen P2 portrait; Stage Select, fight HUD, result/status labels, and opponent names use the selected P2 character where available. The fight loader now loads a separate P2 visual animation clip bank, and world rendering uses it for fighter 2 when available. User-supplied screenshots after `ee1b4ae` on 2026-06-06 confirm simultaneous select presentation, P2 cursor/portrait/name changes across KFM/Evil Ryu/Evil Ken, Evil Ken rendering as the P2 fighter in VS fight, and VS KO/result/match-result presentation. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Current fight runtime still shares P1's CMD/CNS/hitdefs/sounds/constants for runtime execution, so this is independent VS selection plus P2 visual loading rather than full separate P2 moveset/runtime loading. | Full two-character runtime loading replaces the remaining shared-runtime limitation. |
| `[VERIFIED-LIVE]` | Single Player match flow and CPU behavior | Single Player can reach fight presentation and match-complete/result presentation. User-supplied screenshot evidence on 2026-06-05 shows `MATCH COMPLETE`, `SINGLE PLAYER`, Evil Ken winning `2 - 0` by decision on Mountainside Temple. User-supplied screenshots after `ff9f0ba` on 2026-06-06 show Single Player pause during fight, `TIME OVER`, `EVIL RYU WINS`, `MATCH COMPLETE`, Evil Ryu winning `2 - 1` by decision on Mountainside Temple, result-menu `REMATCH` highlighted, and rematch returning to a fresh fight. A minimal CPU baseline now feeds deterministic `FighterInputState` through the normal `updateControlledFighter` -> command/CMD/CNS path. `--verify cpu-baseline` proves P2 moves toward P1, attempts attack state/action `200`, can damage P1 through P2 HitDef resolution, can still be hit/damaged by P1, and keeps timer stability. Advanced AI, specials, personality, and hitbox-debug display in Single Player remain out of scope. | A later CPU/AI system replaces the baseline behavior. |
| `[PARTIAL]` | Training tools | Training has dummy options, hitbox/debug toggles, command HUD/input HUD, move list, power option, guard damage, reset, and P2 control. Live pass verified dummy presence, F1 hitboxes, F2 options, reset, command HUD, input HUD, and a P2 keyboard mode smoke; remaining options need live verification. | Training feature spec is replaced. |
| `[VERIFIED-STATIC]` | Visual Polish Sprint 1 | Render-only presentation polish strengthened fight impact and menu readability without changing gameplay semantics. The pass touched `WorldRender.h`, fight HUD/result overlays, main menu overlay/view assembly, and Training option/command overlays. It adds enlarged additive early-frame fightfx rendering, warmer hit feedback that still respects the Training hit-flash setting, render-only impact shake from existing hitpause and `screenFrame`, combo counter backplates/pulse, stronger round/result bands, improved match-result row hierarchy, a presentation-only `MainMenuView::frame`, and clearer Training option/status/input panels. Full `dev_check` passed from a Visual Studio developer shell with all four verifier summaries at `partial=0 fail=0 blocked=0`; file-size guard still fails only known `App.cpp` hard debt at `8510`. | A later visual pass or art-direction update replaces the presentation treatment. |
| `[PARTIAL]` | Playable visual checkpoint after polish sprint | Manual live smoke on 2026-06-07 after publishing `662792d` observed the polished main menu selection highlight, footer, and exit-confirm prompt; Training route through Character Select and Stage Select; fight HUD entry; command/input HUD readability; F1 hitbox overlay; and F2 Training Options panel hierarchy/status pills. User-supplied controller/keyboard screenshot evidence on 2026-06-07 then confirmed close-range Training hit-impact presentation: KFM hit Dummy with a bright visible spark/warm flash treatment and bottom hit log text (`P1 hit 200#1 dmg 23 ... spark 0 snd 5`) while F1 hitbox outlines were visible. Full `dev_check` was green before publishing with all four runtime verifier summaries at `partial=0 fail=0 blocked=0`. Combo counter pulse, audible sound, render-only shake feel, match-result presentation, Single Player CPU visual smoke, and VS/P2 visual smoke were not rerun in this checkpoint. | A later manual play checkpoint observes combo counter, audible impact, shake feel, match-result presentation, and CPU/VS visual paths directly. |
| `[VERIFIED-LIVE]` | Training command practice usability | The Training command display now uses a compact in-fight practice prompt instead of the earlier oversized command list. It shows the selected move name, parsed input sequence, progressively colored input tokens, current input feedback, and a Show Me prompt. Static Training footer help text is hidden while command training is active to keep the playfield readable. Show Me drives the dummy through parsed `CommandDefinition` / `CommandStateEntry` input steps by producing `FighterInputState` and using the normal controlled-fighter command/CMD/CNS runtime path; it does not hardcode KFM/Evil Ken moves, state numbers, damage, or authored behavior. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke confirmed the compact panel and CPU demo presentation in Training. | A later full command-training mode replaces or extends the practice panel. |
| `[VERIFIED-LIVE]` | Main menu rendering extraction | Render-only main/title menu drawing was originally extracted from `engine/src/App.cpp` and its foreground menu drawing is now implemented as normal module `engine/src/MainMenuOverlay.h/.cpp`. App.cpp still owns resource-backed title background/logo drawing. Post-extraction GUI smoke observed no-argument launch, M.U.G.E.N title art, `DRAGON MUGEN CORE`, Training highlight, footer/help text, Training -> character select -> stage select -> VS -> fight, command/input HUD, visible normal-attack effect, F1 hitboxes, F2 Training Options, `R`, Escape backout, and clean exit. The main menu now has an Exit confirmation prompt so Escape or the Exit row no longer quits without confirmation. This did not change selected-mode mutation, route behavior, settings rendering, root detection, fight loading, CMD/CNS, hit/damage, or sidecar policy. | Full title background/logo resource seam remains separate. |
| `[VERIFIED-STATIC]` | Title/options menu M.U.G.E.N system SND feedback | The title/main menu now loads `system.snd` from the active M.U.G.E.N data folder and reads `[Title Info]` `cursor.move.snd`, `cursor.done.snd`, and `cancel.snd` from `system.def`. Main-menu Up/Down movement plays cursor-move. Valid Training, Single Player, VS Mode, and Options Enter actions play cursor-done after any runtime unload that could clear queued UI audio; the Exit row opens the confirmation prompt with cursor-done, and Escape opens/cancels the prompt with cancel. The Options screen now also plays cursor-move on row movement, cursor-done on adjustable value changes and Back accept, and cancel on Escape. Scripted startup/verifier logs show `SND loaded: system 3 common 10 fight 8`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual audible confirmation was not performed by Codex. | Extend system UI sounds to additional frontend/select screens only when requested. |
| `[VERIFIED-LIVE]` | Options menu rendering extraction | Render-only Options display helpers were extracted from `engine/src/App.cpp` into `engine/src/OptionsMenuOverlay.h`, then later converted to normal module `engine/src/OptionsMenuOverlay.h/.cpp` with AppState-free prepared row data. User-supplied screenshot evidence after the original extraction confirms the Options screen renders with title/header, selected-row highlight, match timer, canvas size, UI scale, pad labels, P1/P2 gamepad rows, and Back row. Settings input handling, value mutation, gamepad assignment behavior, routing, persistence, root detection, input, audio, fight loading, CMD/CNS, hit/damage, controller behavior, resource ownership, and sidecar policy remain in `App.cpp` or existing modules. | Keep settings semantics, gamepad ownership, and title background resources outside the overlay module. |
| `[VERIFIED-LIVE]` | Stage Select rendering extraction | Render-only Stage Select drawing was extracted from `engine/src/App.cpp` into `engine/src/StageSelectOverlay.h`, then later converted to normal module `engine/src/StageSelectOverlay.h/.cpp` with AppState-free prepared view data. Post-extraction GUI smoke observed title -> Training -> KFM -> Stage Select, Stage Select title/arena counter/list/highlight/details/fighter/opponent/footer, Escape back to Character Select, Enter through Stage Select to fight, command/input HUD, F1 hitboxes, F2 Training Options, `R`, Escape backout, and clean exit. Stage selection mutation, Up/Down handling, Enter start behavior, Esc routing, preferred-stage selection, stage loading, VS/fight loading, gameplay runtime, controller status, and sidecar policy remained unchanged. | Keep selection semantics, routing, loading, and resource ownership outside the overlay module. |
| `[VERIFIED-LIVE]` | VS/loading screen rendering extraction | Render-only VS/loading screen drawing was extracted from `engine/src/App.cpp` into `engine/src/VsScreenOverlay.h` and later converted to normal module `engine/src/VsScreenOverlay.h/.cpp` with AppState-free prepared view data. Post-extraction GUI smoke observed Training -> KFM -> Stage Select -> VS/loading screen with mode title, P1/opponent panels, portraits/placeholders, `VS`, stage line, ready/auto-start text, transition to fight, command/input HUD, F1 hitboxes, F2 Training Options, `R`, Escape backout, and clean exit. Fight preparation, loading, selected fighter/stage state, portrait/resource selection, routing, fight startup, gameplay runtime, controller status, and sidecar policy remained unchanged in `App.cpp` or existing modules. | Keep loading, routing, and resource ownership outside the overlay module. |
| `[VERIFIED-LIVE]` | Character Select rendering extraction | Render-only Character Select drawing was extracted from `engine/src/App.cpp` and is now implemented as normal module `engine/src/CharacterSelectOverlay.h/.cpp` with AppState-free prepared view data and non-owning `UiSpriteView` handles. Post-extraction GUI smoke observed Main -> Training -> Character Select with title, KFM/Evil Ryu/Evil Ken icons, KFM portrait/name, Dummy/opponent panel, cursor/highlight, stage label, footer, Enter to Stage Select, Escape from Stage Select back to Character Select, VS/fight transition, command/input HUD, F1 hitboxes, F2 Training Options, `R`, Escape backout, and clean exit. Cursor movement, valid-slot checks, selected character mutation, Enter/Esc routing, `select.def` roster authority, asset loading/cache mutation, preferred-stage selection, stage/fight loading, input, gameplay runtime, controller status, and sidecar policy remained unchanged. Computer Use arrow-key cursor movement was not recorded as PASS because the automation did not move the select cursor. | Keep roster semantics, routing, loading, select-background drawing, and resource ownership outside the overlay module. |
| `[VERIFIED-LIVE]` | Fight HUD rendering extraction | Render-only Fight HUD helpers and `drawFightHud` were extracted from `engine/src/App.cpp` into `engine/src/FightHudOverlay.h`, then later converted to normal module `engine/src/FightHudOverlay.h/.cpp` using prepared `FightHudView` data. Post-extraction GUI smoke observed Training fight HUD names, lifebars, power gauges, `POWER` labels, bottom status/help line, existing command/input HUD, F1 hitboxes, F2 Training Options, `R`, visible normal-attack effect, Escape backout, and clean exit. The normal-module conversion passed build and scripted verifiers but did not rerun manual GUI smoke. Life, damage, power, timer mutation, combo mutation, round flow, hitpause, hitsparks, sounds, input, CMD/CNS, routing, controller status, and sidecar policy remained unchanged. Timer/round pips were not separately live-verified in this pass. | Keep runtime mutation and broader result presentation outside the HUD module. |
| `[VERIFIED-LIVE]` | Fight result overlay rendering extraction | Shared render helpers moved to `engine/src/FightPresentationShared.h`, and render-only round/result presentation moved from `engine/src/App.cpp` into `engine/src/FightResultOverlay.h`, then later converted to normal module `engine/src/FightResultOverlay.h/.cpp` using prepared result views. Post-extraction GUI smoke observed Training -> KFM -> Mountainside Temple -> VS -> fight, Fight HUD names/lifebars/power gauges/`POWER` labels, command/input HUD, F1 hitboxes, F2 Training Options, `R`, one normal input smoke without crash, Escape backout, and clean exit. User-supplied screenshots after `ff9f0ba` on 2026-06-06 provide dedicated live result evidence for Single Player pause, `TIME OVER`, `EVIL RYU WINS`, `MATCH COMPLETE`, Evil Ryu winning `2 - 1` by decision on Mountainside Temple, result-menu `REMATCH`, and rematch returning to fight. Round-state mutation, KO/double-KO decisions, win assignment, match-completion logic, result-menu mutation, rematch/routing, fight loop, input, damage/life/power/timer mutation, CMD/CNS, audio, loading, controller status, and sidecar policy remained unchanged. | Keep result semantics, routing, and runtime mutation outside the result overlay module. |
| `[VERIFIED-LIVE]` | App state type prep extraction | Safe app/UI constants, enums, and pure-data structs were extracted from `engine/src/App.cpp` into normal header `engine/src/AppTypes.h`. Moved definitions include logical/menu/layout constants, `Screen`, `PendingMode`, `OpponentType`, `MatchPhase`, `RoundEndReason`, training/settings enums, `TrainingOptions`, `MainSettings`, `LoadedContentSummary`, `ComboCounterState`, and `FightSessionSlots`. `AppState`, `FighterState`, runtime/resource-owned types, parser/CMD/CNS types, routing, loading, gameplay, input, controller behavior, CMake, and sidecar policy remained unchanged. Post-extraction build, `dev_check`, scripted verifiers, and GUI smoke passed. | A later state/type boundary pass moves the next safe state slice. |
| `[PARTIAL]` | Frontend menu decision boundary extraction | Pure non-fight frontend decision helpers were extracted from `engine/src/App.cpp` into normal module `engine/src/FrontendMenu.h` / `engine/src/FrontendMenu.cpp`. The module decides main/options/character/stage/VS requested actions, pure cursor/index movement, and copied `MainSettings` cycling while `App.cpp` still performs SDL conversion, screen mutation, unloading/loading, fight preparation/startup, gamepad ownership, runtime behavior, input, audio, and sidecar policy. Post-extraction build, `dev_check`, and scripted verifiers passed; GUI smoke observed Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, HUD/command HUD, F1, F2, `R`, Escape backout, and clean exit. Computer Use arrow-key and letter-key injection did not visibly move selectors or trigger a normal during this pass, so Options navigation, selector movement, and normal-input GUI smoke are automation-blocked rather than live-passed here. | A later frontend state split or manual smoke upgrades the blocked selector/options rows. |
| `[VERIFIED-LIVE]` | Frontend menu decision boundary manual smoke | Manual user testing after `4581704` confirms the extracted `FrontendMenu` boundary preserved keyboard and controller frontend behavior, including Character Select movement to Evil Ryu/Evil Ken, Stage Select, VS/loading, fight load, Training overlays, F1/F2, hitbox display, normals/hit evidence, and controller operation. That pass found the air-state caveats now covered by the focused `kfm-air-state` regression verifier. | Frontend menu behavior or physical-input routing changes. |
| `[VERIFIED-LIVE]` | Air-state landing regression fix | A focused runtime fix restored baseline single-jump landing behavior without adding double jump, air dash, Dragon Mode movement, new settings, content edits, or another extraction pass. `--verify kfm-air-state` now proves Up+Forward and Up+Back from neutral and from held walk each produce one finite arc and land with `reentered_air_after_landing=0`, and jump -> air attack lands with `final_y=0`, `final_vy=0`, `final_state=0`, `final_state_type=S`, and `final_on_ground=1`. Existing `kfm-baseline` and `evilken-smoke` verifier results stayed green. Physical keyboard/controller retest is still useful but was not claimed by automation in this pass. | Air movement or landing runtime changes. |
| `[VERIFIED-LIVE]` | Selection state boundary extraction | Character/stage selection metadata was extracted from `engine/src/App.cpp` into normal module `engine/src/SelectionState.h` / `engine/src/SelectionState.cpp`. `AppState` now aggregates `SelectionState`, while `MugenData` still owns `select.def` loading, `CharacterSlot`, `StageSlot`, and character file resolution. Runtime loading, preferred-stage mutation, fight startup, routing, input, CMD/CNS, hit/damage, `FighterState`, editable artifacts, and sidecar policy remained unchanged. Post-extraction build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed; GUI smoke observed Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, HUD/command HUD, F1, F2, `R`, one normal input smoke, Escape backout, and clean exit. Computer Use cursor movement remains automation-blocked, not a runtime failure. | A later frontend/state split replaces more loose AppState groups. |
| `[VERIFIED-LIVE]` | Frontend state boundary extraction | UI/front-end state storage was extracted from `engine/src/App.cpp` into normal header `engine/src/FrontendState.h`. `AppState` now aggregates `FrontendState` and `SelectionState`; routing, screen transitions, `screenFrame` reset/increment behavior, loading, fight startup, quit behavior, input handling, runtime updates, `running`, settings/training/match state, `FighterState`, CPU behavior, and sidecar policy remained unchanged. Post-extraction build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and GUI smoke passed. GUI smoke observed Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, HUD/command HUD, F1, F2, `R`, one normal input smoke, air-state spot check, Escape backout, and clean exit. | A later settings/training state split replaces more loose AppState groups. |
| `[PARTIAL]` | Frontend key flow transitional extraction | Existing key-flow functions `frontendKeyFromSdl`, `settingCycleDirection`, `handleKey`, and `gamepadMenuKeyForButton` were moved from `engine/src/App.cpp` into transitional internal header `engine/src/FrontendFlow.h`. This reduced `App.cpp` from `15266` to `14868` file-size-guard lines. `FrontendFlow.h` remains App.cpp-internal and still depends on AppState and App.cpp-local helpers; runtime/loading/fight helper implementations, SDL event polling, fixed update, render loop, `AppState`, `FighterState`, CPU behavior, and sidecar policy remained unchanged. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke confirmed Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, HUD/command HUD, F1, F2, `R`, normal input, Escape backout, and clean exit. Computer Use still did not move the main selector into Options, so Options/selector movement is automation-blocked for this pass. | A later action-return boundary converts this transitional header into a cleaner normal module. |
| `[VERIFIED-LIVE]` | Training Options behavior extraction | Pure Training Options behavior helpers moved from `engine/src/App.cpp` into normal module `engine/src/TrainingOptionsBehavior.h` / `engine/src/TrainingOptionsBehavior.cpp`. Moved helpers cover dummy guard status/cycling, training power status/cycling, move category status/cycling, Training Options labels/status text, and `toggleTrainingOption` for pure `TrainingOptions` field mutation only. Runtime side effects including power application, reset positions, hit clearing, fighter mutation, F1/F2/R dispatch, routing, loading, CMD/CNS, hit/damage, CPU behavior, controller status, and sidecar policy stayed in `App.cpp` or existing transitional headers. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke confirmed Training route, F2 labels/statuses, toggling and restoring Hitboxes, F1 hitboxes, `R`, Escape backout, and clean exit. | A later TrainingState split replaces more loose AppState training fields. |
| `[VERIFIED-LIVE]` | Shared UI render helpers extraction | Shared render-only UI helpers moved from `engine/src/App.cpp` into transitional internal headers `engine/src/UiRenderPrimitives.h` and `engine/src/UiRenderHelpers.h`. Moved helpers cover color/rect drawing, scaled debug text, UI scale scope, centered/fit debug text, panel drawing, title/select backgrounds, and fixed opponent placeholder rendering. Sprite helpers, texture creation, texture/resource ownership, `TextureSprite`, `SystemScreenAssets`, actor/fighter/stage rendering, runtime effect/projectile rendering, loading, audio, gameplay runtime, CPU behavior, and sidecar policy stayed in `App.cpp` or existing modules. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke confirmed title background, Character Select background, roster icons, fixed Dummy slot, VS/fight route, HUD/command HUD, F1, F2, `R`, normal input, jump spot-check, Escape backout, and clean exit. | A later render/resource boundary replaces the transitional internal-header extraction. |
| `[VERIFIED-LIVE]` | CPU baseline input behavior | A focused runtime bug fix replaced passive Single Player CPU behavior with deterministic CPU input generation and enabled non-dummy P2 attacks to run through the same hit-resolution path as local P2. CPU-controlled P2 now produces ordinary `FighterInputState` and is updated through `updateControlledFighter`, preserving the command buffer and CMD/CNS route into authored character behavior. `--verify cpu-baseline` passed `single_player_fight_phase_ready`, `cpu_moves_toward_p1`, `cpu_attempts_normal_attack`, `cpu_can_damage_p1`, `cpu_can_still_be_hit`, `single_player_timer_stability`, and `clean_exit`. The fix does not hardcode `ChangeState`, animations, damage, specials, combos, character-specific behavior, Dragon Mode, store/crafting, sidecar policy, or `.dragon/*.json`. | A later CPU/AI feature replaces the deterministic baseline. |
| `[VERIFIED-LIVE]` | Match result controller D-pad navigation | Controller confirm/cancel buttons worked on the `MATCH COMPLETE` screen, but user testing found controller D-pad Up/Down did not move the result-menu highlight while keyboard Up/Down did. `FrontendFlow.h` now maps match-result `SDL_GAMEPAD_BUTTON_DPAD_UP/DOWN` to the same `SDLK_UP/DOWN` path as keyboard navigation. User live retest after `4e80ad7` confirmed controller D-pad Up/Down now moves the result-menu highlight and confirm/cancel still work. | Match-result controller routing changes. |
| `[VERIFIED-LIVE]` | Training state boundary extraction | Training Options state storage was extracted from `engine/src/App.cpp` into normal header `engine/src/TrainingState.h`. `AppState` now aggregates `TrainingState training`, with `TrainingState::options` holding the existing `TrainingOptions` defaults from `AppTypes.h`. `lastHitText` and `lastHitTextTicks` intentionally stayed out of `TrainingState` during Pass 19 and were subsequently moved to `FightMessageState`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. User live retest after `4e80ad7` confirmed the Training route, F2 Training Options labels/statuses, F1, `R`, one normal input, diagonal jump landing, air attack landing, Escape backout, clean exit, CPU baseline behavior, and match-result controller D-pad navigation still work. No gameplay, loading, routing, controller behavior, CPU expansion, sidecar policy, content, CMake, Dragon Mode, store/crafting, or `.dragon/*.json` changes were made. | A later broader state boundary replaces more loose AppState fields. |
| `[VERIFIED-STATIC]` | Fight message state boundary extraction | Shared fight-message display state was extracted from `engine/src/App.cpp` into normal header `engine/src/FightMessageState.h`. `AppState` now aggregates `FightMessageState messages`, with `lastHitText` and `lastHitTextTicks` moved under `messages`. Hit/damage calculation, hit contact and guard logic, combo mutation, round-flow decisions, round/result text helpers, HUD rendering, verifier scenario behavior, CPU behavior, loading, audio, resources, `AppState` as a whole, `FighterState`, sidecar policy, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | A later `MatchState` or `RoundState` boundary moves broader match state. |
| `[VERIFIED-STATIC]` | Fight display state boundary extraction | Env-shake display storage and combo counter storage were extracted from `engine/src/App.cpp` into normal header `engine/src/FightDisplayState.h`. `AppState` now aggregates `FightDisplayState display`, with env-shake display fields and `comboCounters` moved under `display`. Env-shake trigger/update behavior, combo mutation functions, camera behavior, global pause behavior, palette/env color, match/round state, HUD rendering, hit/damage/contact/guard logic, CPU behavior, loading, audio, resources, `AppState` as a whole, `FighterState`, sidecar policy, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | A later `MatchState`, `RoundState`, or actor/world rendering boundary moves broader fight systems. |
| `[VERIFIED-STATIC]` | UI render primitive seam | Low-level UI primitives are now a normal module in `engine/src/UiRenderPrimitives.h/.cpp`, with `engine/src/UiRenderContext.h` providing a resource-free render context. Moved helpers include color/rect drawing, scaled rect drawing, SDL debug text, scaled debug text, centered debug text, fit text, panel drawing, `uiScaleFromPercent`, and `ScopedUiScale` using `UiRenderContext`. App.cpp keeps only thin `uiScale` and `uiRenderContext` bridge helpers because `AppState` is still local. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | The seam now supports normal overlay modules. |
| `[VERIFIED-STATIC]` | Pause menu normal module conversion | `PauseMenuOverlay.h` is now a normal declaration header and `PauseMenuOverlay.cpp` owns render-only pause menu labels/layout using `PauseMenuView`, `UiRenderContext`, and public UI primitives. The module no longer takes App.cpp-local `AppState` or `FighterState`. App.cpp still builds the view and owns pause input, selected-option mutation, resume/restart/routing behavior, fight runtime, and screen transitions. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Convert the next low-resource overlay with the same view-struct pattern. |
| `[VERIFIED-STATIC]` | Main menu normal module conversion | `MainMenuOverlay.h` is now a normal declaration header and `MainMenuOverlay.cpp` owns AppState-free foreground menu labels, highlight, description, footer, and title text using `MainMenuView`, `UiRenderContext`, and public UI primitives. App.cpp still owns title background drawing, title logo sprite drawing, sprite helpers, selected-mode state, routing, and `SDL_RenderPresent`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Keep resource-backed title background/logo drawing outside the overlay module. |
| `[VERIFIED-STATIC]` | Stage Select normal module conversion | `StageSelectOverlay.h` is now a normal declaration header and `StageSelectOverlay.cpp` owns AppState-free Stage Select foreground rendering using `StageSelectView`, `StageSelectRowView`, `UiRenderContext`, and public UI primitives. App.cpp still owns stage row assembly, selected-stage metadata lookup, fighter/opponent label preparation, selected-stage state, routing, loading, and `SDL_RenderPresent`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Convert the next low-resource menu overlay with prepared view data. |
| `[VERIFIED-STATIC]` | Options menu normal module conversion | `OptionsMenuOverlay.h` is now a normal declaration header and `OptionsMenuOverlay.cpp` owns AppState-free Options foreground rendering using `OptionsMenuView`, `OptionsMenuRowView`, `UiRenderContext`, and public UI primitives. App.cpp still owns settings state, row/value assembly, gamepad status text, setting mutation, routing, input, persistence, title-background drawing, and `SDL_RenderPresent`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Convert the next low-resource frontend overlay with prepared view data. |
| `[VERIFIED-STATIC]` | VS/loading normal module conversion | `VsScreenOverlay.h` is now a normal declaration header and `VsScreenOverlay.cpp` owns AppState-free VS/loading presentation using `VsScreenView`, `UiSpriteView`, `UiRenderContext`, and public UI primitives. App.cpp still owns portrait/resource selection, selected fighter/stage label assembly, load-status mapping, fight preparation, loading, routing, and `SDL_RenderPresent`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Keep loading, routing, and resource ownership outside the overlay module. |
| `[VERIFIED-STATIC]` | UI sprite view seam | `UiSpriteView.h/.cpp` now provides a public non-owning sprite handle and top-left sprite drawing helper for overlay modules. `VsScreenOverlay` uses this seam for its P1 portrait view. `TextureSprite`, `SystemScreenAssets`, texture creation/destruction, sprite caches, sprite loading, and resource ownership stayed in `App.cpp`; no source-rect support was added because the current `TextureSprite` draw path does not need it. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Convert `CharacterSelectOverlay` using prepared view data plus `UiSpriteView`. |
| `[VERIFIED-STATIC]` | Character Select normal module conversion | `CharacterSelectOverlay.h` is now a normal declaration header and `CharacterSelectOverlay.cpp` owns AppState-free Character Select foreground rendering using `CharacterSelectView`, `CharacterCellView`, `UiRenderContext`, public UI primitives, and `UiSpriteView` handles for portrait/icon/cell/cursor art. App.cpp still owns roster state, selection state, page assembly, sprite/resource lookup, select-background drawing, routing, loading, and `SDL_RenderPresent`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Reassess fight HUD/result or training overlay conversion seams before touching runtime systems. |
| `[VERIFIED-STATIC]` | Fight presentation view seam | `FightPresentationView.h` now provides display-only prepared view structs for fight HUD/result module conversion, including round pips, power gauge, combo counter, fighter HUD, fight HUD, round callout/result, and match-result menu views. `App.cpp` compile-checks the header and remains responsible for assembling all runtime-derived values. `FightHudOverlay` and `FightResultOverlay` now use this seam as normal modules; `FightPresentationShared.h` remains transitional for `singleFightStatusLine`. No gameplay, loading, resources, CPU behavior, controller behavior, sidecar policy, or `.dragon/*.json` changed. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass. | Audit training overlays before converting runtime-adjacent training presentation. |
| `[VERIFIED-STATIC]` | Fight HUD normal module conversion | `FightHudOverlay.h` is now a normal declaration header and `FightHudOverlay.cpp` owns render-only Fight HUD drawing using `FightHudView`, `FighterHudView`, `FightPowerGaugeView`, `FightComboCounterView`, `FightRoundPipsView`, `UiRenderContext`, and public UI primitives. `App.cpp` assembles all runtime-derived strings and primitive values, including selected/opponent labels, life/power values, powerbar positions, combo display values, timer/round pips, hit-log/status line, and gamepad help text. The module has no `AppState`, `FighterState`, fight settings struct, training options, gamepad device, resource ownership, hit/damage, CMD/CNS, or round-flow dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun. | Keep runtime mutation and result semantics outside the HUD module. |
| `[VERIFIED-LIVE]` | Fight Result normal module conversion | `FightResultOverlay.h` is now a normal declaration header and `FightResultOverlay.cpp` owns render-only round start, round finish, round result, and match-result drawing using `FightRoundCalloutView`, `FightRoundResultView`, `FightMatchResultView`, `UiRenderContext`, and public UI primitives. `App.cpp` assembles all runtime-derived strings and primitive values, including timing-gated callout visibility, round result text, round pips, match winner text, score/method text, victory quote, stage text, and result-menu selected rows. The module has no `AppState`, `FighterState`, match settings struct, routing state, resource ownership, hit/damage, CMD/CNS, CPU, loading, audio, controller, or round-flow dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. User-supplied screenshots after `ff9f0ba` on 2026-06-06 confirmed dedicated Single Player round/result/match-result and rematch live behavior. | Keep result semantics and runtime mutation outside the result overlay module. |
| `[VERIFIED-STATIC]` | Training Options normal module conversion | `TrainingOptionsOverlay.h` is now a normal declaration header and `TrainingOptionsOverlay.cpp` owns render-only F2 Training Options and command move-list drawing using `TrainingOptionsMenuView`, `TrainingMoveListView`, `UiRenderContext`, and public UI primitives. `App.cpp` assembles all labels, statuses, move rows, detail text, selected character text, category text, and page labels from existing training options and command-entry display helpers. The module has no `AppState`, `FighterState`, `CommandStateEntry`, resource ownership, command matching, CMD/CNS, hit/damage, dummy behavior, input routing, or runtime side-effect dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual F2/move-list GUI smoke was not rerun. | Create a command/input HUD view seam before converting `TrainingCommandOverlay`. |
| `[VERIFIED-STATIC]` | Training Command HUD view seam | `TrainingCommandView.h` now provides display-only prepared view structs for the command/input HUD: `TrainingCommandRowView`, `TrainingInputHudView`, and `TrainingCommandHudView`. `App.cpp` compile-checks the seam only; `TrainingCommandOverlay.h` remains transitional and call sites are unchanged. The seam has no `AppState`, `FighterState`, `CommandStateEntry`, command definitions, input history objects, resource ownership, command matching, CMD/CNS, hit/damage, input routing, or runtime side-effect dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun. | Convert `TrainingCommandOverlay` using prepared `TrainingCommandHudView` data assembled in `App.cpp`. |
| `[VERIFIED-STATIC]` | Training Command normal module conversion | `TrainingCommandOverlay.h` is now a normal declaration header and `TrainingCommandOverlay.cpp` owns render-only command/input HUD drawing using `TrainingCommandHudView`, `TrainingInputHudView`, `TrainingCommandRowView`, `UiRenderContext`, and public UI primitives. `App.cpp` assembles current input text, recent input text, visible command rows, row active flags, and active command labels from existing input history and command display helpers. The module has no `AppState`, `FighterState`, `CommandStateEntry`, command definitions, input history objects, resource ownership, command matching, CMD/CNS, hit/damage, input routing, or runtime side-effect dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual command/input HUD GUI smoke was not rerun. | Convert `TrainingDebugOverlay` using prepared screen-space debug view data assembled in `App.cpp`. |
| `[VERIFIED-STATIC]` | Training Debug view seam | `TrainingDebugView.h` now provides display-only prepared view structs for the F1 debug overlay: `TrainingDebugColorView`, `TrainingDebugLineView`, `TrainingDebugRectView`, `TrainingDebugReadoutView`, and `TrainingDebugView`. The seam has no `AppState`, `FighterState`, `StageSlot`, `AnimationFrame`, `CollisionBox`, camera/stage runtime object, debug clipboard, resource ownership, command matching, CMD/CNS, hit/damage, input routing, or runtime side-effect dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual F1 debug GUI smoke was not rerun. | Reassess the internal debug view assembly bridge with remaining transitional helpers. |
| `[VERIFIED-STATIC]` | Training Debug normal module conversion | `TrainingDebugOverlay.h` is now a normal declaration header and `TrainingDebugOverlay.cpp` owns render-only F1 debug overlay drawing using `TrainingDebugView`, `TrainingDebugLineView`, `TrainingDebugRectView`, `TrainingDebugReadoutView`, `UiRenderContext`, and public UI primitives. `TrainingDebugViewAssembly.h` remains App.cpp-internal and prepares screen-space floor/origin lines, hitbox rectangles, and readout strings from existing fighter/stage/animation/camera/debug clipboard data. The module has no `AppState`, `FighterState`, `StageSlot`, `AnimationFrame`, `CollisionBox`, debug clipboard, resource ownership, command matching, CMD/CNS, hit/damage, input routing, or runtime side-effect dependency. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual F1 debug GUI smoke was not rerun. | Audit remaining transitional helpers before converting another runtime-adjacent boundary. |
| `[VERIFIED-STATIC]` | Remaining transitional helper audit | `docs/TRANSITIONAL_HELPER_BOUNDARY_AUDIT.md` records the remaining App.cpp-internal helper and bridge files after overlay conversion: `FightPresentationShared.h`, `UiRenderHelpers.h`, `TrainingDebugViewAssembly.h`, `FrontendFlow.h`, and `AppVerificationBridge.h`. The audit recommends retiring `FightPresentationShared.h` first, then reassessing the `UiRenderHelpers.h` background/sprite presentation seam. `FrontendFlow.h`, `TrainingDebugViewAssembly.h`, and `AppVerificationBridge.h` remain deferred behind action/runtime boundaries. No source, CMake, content, runtime, controller, sidecar, or `.dragon/*.json` changes were made. | Pass 42 retires the smallest remaining transitional helper without moving runtime systems. |
| `[VERIFIED-STATIC]` | Frontend/Input helper implementation move | Pure helper implementations for mode labels, opponent labels, match-mode classification, settings display labels/status text, compact UI text, match-timer tick conversion, and gamepad family text now live in `FrontendMenu.cpp` / `Input.cpp` instead of `App.cpp`. This reduced `App.cpp` by 120 file-size-guard lines without changing routing, input collection, gamepad ownership, settings mutation, loading, runtime behavior, controller behavior, sidecar policy, or `.dragon/*.json`. | Continue only with App.cpp-reducing implementation moves. |
| `[VERIFIED-STATIC]` | World rendering body move | World/stage/actor/effect/projectile rendering bodies now live in App.cpp-internal implementation header `engine/src/WorldRender.h`: `drawStageLayer`, `drawFallbackStage`, palette-effect render helpers, `drawPaletteOverlay`, `drawActor`, `drawFighter`, `drawRuntimeEffect`, `drawRuntimeProjectile`, and `drawWorldActors`. `drawFightView` remains in `App.cpp` as the fight-screen coordinator. This reduced `App.cpp` from `14917` to `14510` file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. No gameplay, runtime mutation, loading/resource ownership, texture ownership, CPU/controller behavior, sidecar policy, boss intro behavior, or `.dragon/*.json` changed. | Continue with completed App.cpp-reducing implementation moves, not seam-only passes. |
| `[VERIFIED-STATIC]` | Audio runtime body move | Audio sample decoding, SDL audio stream init/shutdown, common/fight/character sample lookup, channel stop/low-priority logic, duplicate-sound suppression, active voice capping, voice playback, mixing, and mixer queue update now live in App.cpp-internal implementation header `engine/src/AudioRuntime.h`. Gameplay decisions about when sounds play, PlaySnd/StopSnd controller evaluation, hit/guard sound trigger decisions, CMD/CNS, hit/damage, round flow, loading/resource ownership outside audio, controller behavior, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `14510` to `14217` file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Continue with completed App.cpp-reducing implementation moves. |
| `[VERIFIED-STATIC]` | Runtime loading body move | Character sprite, system sprite, stage background, content summary, preferred-stage, selected character runtime load/unload, visual asset load/destroy, texture destruction, and animation clip destruction helpers now live in App.cpp-internal implementation header `engine/src/RuntimeLoading.h`. `prepareFightSession` and `beginFight` remain in `App.cpp`; loading order, fight reset/start semantics, resource ownership policy, `select.def` authority, sidecar policy, CMD/CNS, hit/damage, round flow, CPU/controller behavior, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `14217` to `13839` file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Continue with completed App.cpp-reducing implementation moves, with runtime systems still deferred. |
| `[VERIFIED-STATIC]` | Command parsing body move | Command definition parsing, command atom/step parsing, State -1 command-state entry loading, command trigger condition parsing, command option groups, and command file default timing parsing now live in App.cpp-internal implementation header `engine/src/CommandParsing.h`. Live command recognition, input history runtime, `FighterState`, State -1 runtime routing, CMD/CNS execution, `ChangeState`/`SelfState` behavior, hit/damage, round flow, CPU/controller behavior, loading/routing outside command file parsing, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `13839` to `13434` file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Continue with completed App.cpp-reducing implementation moves while keeping live CMD/CNS runtime deferred. |
| `[VERIFIED-STATIC]` | Command recognition body move | Live command recognition and input-buffer matching now live in App.cpp-internal implementation header `engine/src/CommandRecognition.h`. The moved code covers input-history frame push/read helpers, button/direction atom matching, release/fresh/hold command step matching, buffered command definition matching, and `collectFighterCommands`. Command-state eligibility, command target resolution, State -1 routing, CNS execution, `enterState`, movement update, hit/damage, round flow, CPU/controller behavior, input device mapping, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `10356` to `10092` file-size-guard lines; `CommandRecognition.h` is 272 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Continue with completed App.cpp-reducing implementation moves while keeping State -1 routing and live CNS execution guarded. |
| `[VERIFIED-STATIC]` | Command state eligibility body move | Command-state eligibility and active command selection now live in App.cpp-internal implementation header `engine/src/CommandStateEligibility.h`. The moved code covers command condition value lookup, command `=` / `!=` expression replacement, target-state resolution, required/optional/forbidden command gates, `ctrl` / `movecontact` gates, state type/range/expression gates, state/animation availability checks, and read-only active command entry selection. `applyCommandState`, State -1 routing, state transition behavior, CNS execution, `enterState`, movement update, hit/damage, round flow, CPU/controller behavior, input device mapping, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `10092` to `9922` file-size-guard lines; `CommandStateEligibility.h` is 166 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Continue with completed App.cpp-reducing implementation moves while keeping state transitions and live CNS execution guarded. |
| `[VERIFIED-STATIC]` | Under-10k architecture checkpoint and runtime-controller audit | `App.cpp` is now under 10k file-size-guard lines after the completed body-cut sequence: world rendering, audio runtime, runtime loading, command parsing, state-controller parsing, fight/session setup, command recognition, and command-state eligibility. The retired runtime-controller checkpoint distinguished read-only expression/trigger evaluation from runtime mutation. Fresh `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` verifier runs stayed green. No source, CMake, content, sidecar, boss intro, or `.dragon/*.json` changes were made in this checkpoint. | Completed by the read-only runtime expression/trigger evaluation body move. |
| `[VERIFIED-STATIC]` | Runtime expression evaluation body move | Read-only runtime expression and trigger evaluation now live in App.cpp-internal implementation header `engine/src/RuntimeExpressionEvaluation.h`. The moved code covers comparison helpers, current-command reads, variable reads, deterministic random/value conversion helpers, P2 distance and edge-distance values, M.U.G.E.N expression parsing/evaluation, expression conditions, trigger groups, `stateControllerTriggerActive`, and `simpleControllerTriggerActive`. `setFighterVariableValue`, controller fired-history helpers, `shouldRunStateRuntimeController`, all controller execution bodies, `enterState`, hit/damage/guard, helper/projectile/explod lifecycle, target mutation, round flow, CPU/controller behavior, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `9922` to `9135` file-size-guard lines; `RuntimeExpressionEvaluation.h` is 795 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. | Audit controller execution breakdown before moving mutation-heavy runtime bodies. |
| `[VERIFIED-STATIC]` | Runtime controller execution split audit | The retired runtime-controller execution split checkpoint classified the remaining controller execution body after read-only runtime expression evaluation moved. It separated controller persistence, audio, assert-special, visual/display, variable/meter, movement, pause, helper/projectile/explod, state transition, target, hit/damage, and round-flow risks. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. No source, CMake, content, runtime, sidecar, boss intro, or `.dragon/*.json` changes were made. The original recommendation was completed later by `de22bec`. | Historical context only; the current roadmap now records the completed utility, variable, PowerAdd, velocity, PosAdd, PosSet, SprPriority, PosFreeze, Turn, and related cuts and points next to CtrlSet / command-control audit. |
| `[VERIFIED-STATIC]` | Architecture recovery roadmap re-evaluation audit | `docs/ARCHITECTURE_RECOVERY_ROADMAP_AUDIT.md` superseded older "next work" audit conclusions at the pre-utility checkpoint. It recorded the `App.cpp` reduction from `16820` to `9184` file-size-guard lines, identified runtime-core extraction as the current phase, and named `StateControllerUtilityRuntime.h` as the next code pass. That recommendation was completed later by `de22bec`, which moved the controller utility/audio/assert/display body. No source, CMake, content, runtime behavior, controller behavior, sidecar policy, or `.dragon/*.json` changed in the original docs-only audit. | Historical checkpoint now superseded by the current `8507`-line roadmap baseline and PosFreeze / movement-freeze audit recommendation after the PosSet body move. |
| `[VERIFIED-STATIC]` | State controller utility runtime body move | `engine/src/StateControllerUtilityRuntime.h` now contains the bounded utility/audio/assert/display controller runtime body. The moved code covers audio fire-key helpers and PlaySnd/StopSnd execution, AssertSpecial flag helpers, display/clipboard/victory quote helpers, palette/remap/trans/afterimage visual controllers, `changeAnimTriggerActive`, the force-feedback helper, afterimage effect ticking, and the shared runtime fired-history gate as a placement move. `App.cpp` dropped from `9184` to `8632` file-size-guard lines; `StateControllerUtilityRuntime.h` is `560` lines. Build, full `dev_check`, `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=11 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`) passed. `CtrlSet`, variable/meter controllers, movement/position controllers, `ChangeAnim` execution, pause/superpause, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, target controllers, HitDef/damage/guard, round flow, CPU behavior, input routing, content loading, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. | Variable controller body, meter/stat audit, PowerAdd body move, movement/position audit, velocity body move, position/grounding audit, and PosAdd body move are now complete. |
| `[VERIFIED-STATIC]` | Variable controller runtime body move | `engine/src/StateControllerVariableRuntime.h` now contains only `updateStateVariableControllers(...)`, covering `VarSet`, `VarAdd`, `VarRandom`, and `VarRangeSet` execution. `setFighterVariableValue(...)` stayed in `App.cpp` because helper-owned `ParentVarAdd` still calls it. `ParentVarAdd`, `PowerAdd`, `LifeAdd`, `HitAdd`, `AttackDist`, `AttackMulSet`, `DefenceMulSet`, movement/position controllers, pause/superpause, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, target controllers, HitDef/damage/guard, round flow, CPU behavior, input routing, content loading, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. `App.cpp` dropped from `8632` to `8571` file-size-guard lines; `StateControllerVariableRuntime.h` is `69` lines. Build, full `dev_check`, `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=11 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`) passed. | Meter/stat audit, PowerAdd body move, movement/position audit, velocity body move, position/grounding audit, and PosAdd body move are now complete. |
| `[VERIFIED-STATIC]` | Meter/stat controller runtime audit | The retired meter/stat controller checkpoint classified the remaining meter/stat controller body after `6b04904`. `PowerAdd` was the only controller marked `READY WITH LIMITS`, because it mutates `fighter.power` and clamps to `[0, maxPower]` without directly touching hit/damage, target links, lifecycle, state transitions, or round flow. `LifeAdd` is deferred to a round-flow seam; `HitAdd`, `AttackDist`, `AttackMulSet`, and `DefenceMulSet` are deferred to hit/contact or hit/damage seams. This was docs-only; `App.cpp` remained `8571` lines, and source, CMake, content, sidecar policy, branch topology, boss intro behavior, and `.dragon/*.json` stayed unchanged. `dev_check.py --skip-build` passed; file-size guard still failed only known `App.cpp` hard debt. | Completed by the `PowerAdd`-only body move; do not move the other meter/stat controllers without a separate seam audit. |
| `[VERIFIED-STATIC]` | PowerAdd controller runtime body move | `engine/src/StateControllerPowerRuntime.h` now contains only `updateStatePowerControllersForDefinition(...)`, which owns the `PowerAdd` loop from `updateStateMeterControllers(...)`. It preserves the original `shouldRunStateRuntimeController(...)` call, expression evaluation, and clamp to `[0, maxPower]`. `LifeAdd`, `HitAdd`, `AttackDist`, `AttackMulSet`, `DefenceMulSet`, `TargetPowerAdd`, `TargetLifeAdd`, HitDef/damage/guard, KO/time-over/double-KO/round flow, movement/position, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU behavior, sidecar policy, and `.dragon/*.json` stayed unchanged. `App.cpp` dropped from `8571` to `8561` file-size-guard lines; `StateControllerPowerRuntime.h` is `27` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=11 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). | Movement/position audit and velocity body move are now complete; still avoid `LifeAdd`, damage-adjacent controllers, and round flow. |
| `[VERIFIED-STATIC]` | Movement/position controller runtime audit | The retired movement/position controller checkpoint classified movement, position, control, state-shape, bounds, and adjacent hit/get-hit controller execution after `ff73242`. `VelSet`, `VelAdd`, and `VelMul` were marked `READY WITH LIMITS` and then completed by the `StateControllerVelocityRuntime.h` body move. `PosAdd`, `PosSet`, `PosFreeze`, `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, `SprPriority`, `Turn`, `HitVelSet`, `HitFallVel`, `HitFallSet`, `HitFallDamage`, `HitBy`, `NotHitBy`, and `HitOverride` were deferred to narrower position, grounding, control, state-shape, camera/contact, hit/get-hit, or damage seams. This was originally docs-only; `App.cpp` remained `8561` lines at audit time, and source, CMake, content, sidecar policy, branch topology, boss intro behavior, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. `dev_check.py --skip-build` passed. | Completed by the later narrow body moves through Turn; do not move control/state-shape/bounds/hit-get-hit controllers without the next focused audit. |
| `[VERIFIED-STATIC]` | Velocity controller runtime body move | `engine/src/StateControllerVelocityRuntime.h` now contains only `updateStateVelocityControllersForDefinition(...)`, which owns the `VelSet` / `VelAdd` / `VelMul` loop from `updateStateMovementControllers(...)`. It preserves the original `shouldRunStateRuntimeController(...)` call, operation switch, facing-aware X semantics, and current assignment/add/multiply behavior for `fighter.vx` and `fighter.vy`. `PosAdd`, `PosSet`, `PosFreeze`, `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, `SprPriority`, `Turn`, hit/get-hit adjacent controllers, HitDef/damage/guard, KO/time-over/double-KO/round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU behavior, input routing, sidecar policy, and `.dragon/*.json` stayed unchanged. `App.cpp` dropped from `8561` to `8533` file-size-guard lines; `StateControllerVelocityRuntime.h` is `43` lines. Full `dev_check` passed from a Visual Studio developer shell with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=11 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). | Position/grounding audit and PosAdd body move are now complete; next target should audit `PosSet` / grounding and still avoid hit/damage, round flow, lifecycle, target, and pause/superpause behavior. |
| `[VERIFIED-STATIC]` | Position/grounding controller runtime audit | The retired position/grounding controller checkpoint classified `PosAdd`, `PosSet`, and `PosFreeze` after the velocity-only body move. `PosAdd` was marked `READY WITH LIMITS` because it adds facing-aware X and direct Y offsets with separate simple-trigger fired history. `PosSet` was deferred because it sets absolute position and updates `onGround`; `PosFreeze` was deferred to movement-freeze work. This was docs-only; `App.cpp` remained `8533` lines, and source, CMake, content, sidecar policy, branch topology, boss intro behavior, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. `dev_check.py --skip-build` passed; file-size guard still failed only known `App.cpp` hard debt. | Completed by the PosAdd, PosSet, and PosFreeze body moves. |
| `[VERIFIED-STATIC]` | PosAdd controller runtime body move | `engine/src/StateControllerPosAddRuntime.h` now contains only `updateStatePosAddControllersForDefinition(...)`, which owns the `PosAdd` loop from `updateStatePosAddControllers(...)`. It preserves trigger checks, simple-trigger fired-history behavior, facing-aware X adjustment, and direct X/Y addition. `statePosAddAlreadyFired(...)` and `markStatePosAddFired(...)` stayed in `App.cpp`. `App.cpp` dropped from `8533` to `8517` file-size-guard lines; `StateControllerPosAddRuntime.h` is `31` lines. Full `dev_check` passed from a Visual Studio developer environment with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=11 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). `PosSet`, `PosFreeze`, control/state-shape controllers, collision/contact-adjacent controllers, hit/get-hit controllers, HitDef/damage/guard, KO/time-over/double-KO/round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU/input behavior, sidecar policy, content, and `.dragon/*.json` stayed unchanged. | Completed by the PosSet / grounding audit, verifier hardening, and PosSet-only body move. |
| `[VERIFIED-STATIC]` | PosSet / grounding controller runtime audit | The retired PosSet / grounding checkpoint audited the live `PosSet` body after `d933a4d`. The body only gates through `shouldRunStateRuntimeController(...)`, assigns optional absolute `fighter.x`, assigns optional absolute `fighter.y`, and updates `fighter.onGround` from `fighter.y >= 0.0f`. The checkpoint recorded that landing clamp/common landing, state-zero movement, hit/get-hit landing, `StateTypeSet`, `PosFreeze`, and player push are related but separate seams. At that time, `kfm-air-state` proved generic diagonal jump and air attack landing but not an authored PosSet-triggered grounding path. This was docs-only; `App.cpp` remained `8517` lines, and source, CMake, content, sidecar policy, branch topology, boss intro behavior, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. | Completed by the PosSet-backed grounding verifier hardening row. |
| `[VERIFIED-LIVE]` | PosSet-backed grounding verifier hardening | `kfm-air-state` now includes `kung_fu_knee_posset_grounding`. The row drives KFM's authored `FF_a` path through normal symbolic input, CMD command recognition, CNS `ChangeState`, states `1050 -> 1051 -> 1052`, the state `1052` `PosSet` controller, `y=0`, `onGround=1`, no airborne final state, and return to controllable idle. The first strict row exposed a real no-physics grounding gap: KFM entered `1050` and `1051` but did not reach `1052`. The scoped fix prevents custom `physics = N` air states from being implicitly ground-clamped or auto-finished to idle before authored landing controllers can run. No controller execution moved in that pass; `PosSet`, `PosAdd`, `PosFreeze`, `StateTypeSet`, `CtrlSet`, hit/get-hit controllers, HitDef/damage/guard, round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, and pause/superpause controller bodies stayed in `App.cpp`. `App.cpp` remained `8517` file-size-guard lines; `VerificationScenario.cpp` is `741` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=12 partial=0 fail=0 blocked=0`), and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). | Completed by the PosSet-only body move; keep `PosFreeze`, state-shape/control, hit/damage, round flow, lifecycle, target, and pause/superpause work deferred. |
| `[VERIFIED-STATIC]` | PosSet controller runtime body move | `engine/src/StateControllerPosSetRuntime.h` now contains only `updateStatePosSetControllersForDefinition(...)`, which owns the `PosSet` loop from `updateStateMovementControllers(...)`. It preserves `shouldRunStateRuntimeController(...)`, optional absolute `fighter.x` assignment, optional absolute `fighter.y` assignment, and `fighter.onGround = fighter.y >= 0.0f`. `App.cpp` dropped from `8517` to `8507` file-size-guard lines; `StateControllerPosSetRuntime.h` is `24` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=12 partial=0 fail=0 blocked=0`) including `kung_fu_knee_posset_grounding`, and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). File-size guard still fails only known `App.cpp` hard debt. `PosAdd`, `PosFreeze`, `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, `SprPriority`, `Turn`, velocity controllers, hit/get-hit controllers, HitDef/damage/guard, round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU/input behavior, sidecar policy, grounding physics, content, and `.dragon/*.json` stayed unchanged. | Completed by the misc movement/state controller audit; next target is `SprPriority` only. |
| `[VERIFIED-STATIC]` | Misc movement/state controller runtime audit | The retired misc movement/state checkpoint classified the remaining smaller movement/state-shape controller group after `2a096fb`. `SprPriority` was marked `READY WITH LIMITS` because it only assigns `fighter.sprPriority` behind the standard runtime gate. `PosFreeze` was noted as movement-freeze behavior, `Turn` as facing/jump-action behavior, `CtrlSet` as control/runtime behavior with custom fired-history semantics, `StateTypeSet` as state-shape/physics/grounding behavior, and `ScreenBound`, `Width`, and `PlayerPush` as camera/bounds or contact/collision seams. This was docs-only; `App.cpp` remained `8507` lines, and source, CMake, content, sidecar policy, branch topology, boss intro behavior, Dragon Mode, store/crafting, and `.dragon/*.json` stayed unchanged. `dev_check.py --skip-build` passed; file-size guard still failed only known `App.cpp` hard debt. | Completed by the SprPriority, PosFreeze, and Turn body moves; do not bundle control/state-shape, bounds/contact, hit/damage, round-flow, lifecycle, target, or pause/superpause work. |
| `[VERIFIED-STATIC]` | SprPriority controller runtime body move | `engine/src/StateControllerSprPriorityRuntime.h` now contains only `updateStateSprPriorityControllersForDefinition(...)`, which owns the `SprPriority` loop from `updateStateMovementControllers(...)`. It preserves `shouldRunStateRuntimeController(...)` and `fighter.sprPriority = sprPriority.value`. `App.cpp` dropped from `8507` to `8503` file-size-guard lines; `StateControllerSprPriorityRuntime.h` is `18` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=12 partial=0 fail=0 blocked=0`) including `kung_fu_knee_posset_grounding`, and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). File-size guard still fails only known `App.cpp` hard debt. `PosFreeze`, `Turn`, `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, velocity controllers, `PosAdd`, `PosSet`, hit/get-hit controllers, HitDef/damage/guard, round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU/input behavior, sidecar policy, grounding physics, content, and `.dragon/*.json` stayed unchanged. | Completed by the PosFreeze-only body move. |
| `[VERIFIED-STATIC]` | PosFreeze controller runtime body move | `engine/src/StateControllerPosFreezeRuntime.h` now contains only `updateStatePosFreezeControllersForDefinition(...)`, which owns the `PosFreeze` loop from `updateStateMovementControllers(...)`. It preserves `shouldRunStateRuntimeController(...)`, trigger/persistence behavior, and the original `fighter.posFreezeX = fighter.posFreezeX || posFreeze.freezeX` / `fighter.posFreezeY = fighter.posFreezeY || posFreeze.freezeY` semantics. `App.cpp` dropped from `8503` to `8498` file-size-guard lines; `StateControllerPosFreezeRuntime.h` is `21` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=12 partial=0 fail=0 blocked=0`) including `kung_fu_knee_posset_grounding`, and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). File-size guard still fails only known `App.cpp` hard debt. `Turn`, `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, velocity controllers, `PosAdd`, `PosSet`, hit/get-hit controllers, HitDef/damage/guard, round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU/input behavior, sidecar policy, grounding physics, physics consumption of freeze flags, per-tick freeze-flag reset, content, and `.dragon/*.json` stayed unchanged. | Completed by the Turn-only body move. |
| `[VERIFIED-STATIC]` | Turn controller runtime body move | `engine/src/StateControllerTurnRuntime.h` now contains only `updateStateTurnControllersForDefinition(...)`, which owns the `Turn` loop from `updateStateMovementControllers(...)`. It preserves `shouldRunStateRuntimeController(...)`, trigger/persistence behavior, `fighter.facing *= -1`, `fighter.jumpBaseAction = 0`, and `fighter.jumpPeakActionApplied = false`. `App.cpp` dropped from `8498` to `8492` file-size-guard lines; `StateControllerTurnRuntime.h` is `22` lines. Full `dev_check` passed with `kfm-baseline` (`pass=12 partial=0 fail=0 blocked=0`), `evilken-smoke` (`pass=9 partial=0 fail=0 blocked=0`), `kfm-air-state` (`pass=12 partial=0 fail=0 blocked=0`) including `kung_fu_knee_posset_grounding`, and `cpu-baseline` (`pass=7 partial=0 fail=0 blocked=0`). File-size guard still fails only known `App.cpp` hard debt. `CtrlSet`, `StateTypeSet`, `ScreenBound`, `Width`, `PlayerPush`, velocity controllers, `PosAdd`, `PosSet`, hit/get-hit controllers, HitDef/damage/guard, round flow, helper/projectile/explod lifecycle, `ChangeState`/`SelfState`, pause/superpause, CPU/input behavior, input routing, command recognition, opponent-relative behavior, sidecar policy, content, and `.dragon/*.json` stayed unchanged. | Next target is audit-first for `CtrlSet` / command-control behavior. |
| `[VERIFIED-STATIC]` | CtrlSet / command-control controller runtime audit | `docs/CTRLSET_COMMAND_CONTROL_CONTROLLER_RUNTIME_AUDIT.md` classifies the live `updateStateCtrlControllers(...)` body before any source movement. The body evaluates parsed or simple triggers, uses `firedStateCtrlControllerIds` through `stateCtrlAlreadyFired(...)` / `markStateCtrlFired(...)`, marks the controller fired before assignment, and mutates only `fighter.ctrl = ctrl.value`. The audit records that `fighter.ctrl` is command-control coupled because `canEnterCommandState(...)` rejects `requiresCtrl` entries while control is false, fallback walk/jump checks require control, `cpuCanChooseInput(...)` requires control, helpers run the controller too, and round-finish paths can force control off outside `CtrlSet`. Existing verifiers prove broad controllable idle and command behavior but do not prove an authored `CtrlSet` toggles control and restores command eligibility. This was docs-only; `App.cpp` remains `8510` file-size-guard lines, and source, CMake, content, sidecar policy, gameplay behavior, branch topology, and `.dragon/*.json` stayed unchanged. | Next target is command-control verifier hardening before any `CtrlSet` source movement. |
| `[VERIFIED-LIVE]` | State-controller parsing body move | CNS state definition loading, state-controller trigger parsing, controller parameter parsing, state type / move type / physics parsing, and controller data population now live in App.cpp-internal implementation header `engine/src/StateControllerParsing.h`. Runtime trigger evaluation during fight, live controller execution, `ChangeState` / `SelfState` behavior, `FighterState` mutation, CMD/CNS runtime routing, hit/damage/guard, round flow, CPU/controller behavior, loading/routing outside CNS parsing, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. This reduced `App.cpp` from `13496` to `10617` file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. User live retest after `42892c9` confirmed main/menu route, Options, Training Options, command list, hitboxes, fight rounds, CPU win/result presentation, and general gameplay checks still work. Options/Training Options wording can still be visually cut off, but no functional regression was observed. | Continue with completed App.cpp-reducing implementation moves while keeping live controller execution and hit/damage deferred. |
| `[VERIFIED-STATIC]` | State-controller parser header split | The oversized 2888-line `StateControllerParsing.h` internal parser header is now a 10-line coordinator that includes smaller App.cpp-internal parser headers: trigger parsing, controller parameter parsing, state definition loading, and bounded controller-loading handler fragments. The largest new split parser file is `StateControllerTriggerParsing.h` at 721 lines, and file-size guard now fails only on known `App.cpp` hard debt. `App.cpp` stayed at `10617` lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. No runtime trigger evaluation, live controller execution, `FighterState`, CMD/CNS runtime routing, hit/damage/guard, round flow, CPU/controller behavior, loading/routing outside CNS parsing, sidecar policy, boss intro behavior, or `.dragon/*.json` changed. | Continue with completed App.cpp-reducing implementation moves after this parser-debt cleanup. |
| `[VERIFIED-STATIC]` | Fight session setup body move | Fight/session setup and reset body code now lives in App.cpp-internal implementation header `engine/src/FightSessionRuntime.h`. The moved code covers session slot setup, round initial state entry, fighter variable clearing, global pause/env-shake/palette clear helpers, Training reset positions, fight round reset, fight state reset, fight-session preparation, fight begin, and delayed VS preparation. `App.cpp` dropped from `10617` to `10356` file-size-guard lines; `FightSessionRuntime.h` is 266 file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Live fight update, runtime trigger evaluation, controller execution, hit/damage/guard, KO/time-over decisions, round-flow update logic, CPU/controller behavior, input handling, `FighterState`, `AppState`, resource ownership, sidecar policy, boss intro behavior, and `.dragon/*.json` stayed unchanged. | Continue with completed App.cpp-reducing implementation moves while keeping live runtime systems deferred. |
| `[VERIFIED-LIVE]` | Training overlay rendering extraction | Render-only Training overlay drawing for F1 hitboxes/debug, command/input HUD, and F2 options/move-list pages was extracted from `engine/src/App.cpp` into focused internal headers. Post-extraction GUI smoke observed title -> Training -> character select -> stage select -> VS -> fight, command/input HUD rendering, F1 hitboxes, F2 Training Options, fight backout, and clean exit. This did not change input, training option mutation, dummy behavior, CMD/CNS, hit/damage, audio, or routing logic. | A later module split replaces the transitional internal-header extraction. |
| `[PARTIAL]` | Single fight rules | Single Fight/VS fight presentation, timer/HUD, pause/resume, and local Player 2 fight controls have live evidence. Console hit logs from user testing show P1 and P2 hit events in an Evil Ryu local fight. VS Mode now supports simultaneous P1/P2 character selection, P2 portrait/name presentation before Stage Select, and separate P2 visual animation clips during fight rendering when available. User-supplied screenshots after `ff9f0ba` on 2026-06-06 verify Single Player time-over, round result, match result, result-menu `REMATCH`, and rematch returning to a fresh fight. User-supplied screenshots after `ee1b4ae` on 2026-06-06 verify VS final-round fight presentation with Evil Ryu vs Evil Ken, KO/result presentation, and a VS `MATCH COMPLETE` result menu with `REMATCH` highlighted. Full separate P2 CMD/CNS/hitdef/sound/constants runtime loading, KO/double-KO/draw handling, and the non-Resume pause actions still need dedicated work/checks. | Fight rules feature spec is replaced. |
| `[VERIFIED-LIVE]` | Pause menu rendering extraction | Render-only match pause menu label/layout drawing was originally extracted from `engine/src/App.cpp` and is now implemented as normal module `engine/src/PauseMenuOverlay.h/.cpp`. The extraction moved no pause input handling, selected-option mutation, resume/restart actions, routing, match reset, or fight runtime behavior. Build, dev checks, file-size guard, and scripted verifiers passed afterward. User-supplied screenshots on 2026-06-04 confirm the Single Player and VS Mode pause menus render with `RESUME`, `RESTART MATCH`, `FIGHTER SELECT`, `STAGE SELECT`, and `MODE SELECT`, and that `RESUME` returns to the fight. Other pause actions still need individual live checks. | Additional pause actions need dedicated live checks before broader claims. |
| `[VERIFIED-LIVE]` | Input boundary | Keyboard and SDL3 gamepad mappings are isolated in the extracted input boundary. P1 keyboard, P2 keyboard, and DualSense/PlayStation controller smoke are verified-live. User evidence shows PlayStation pad labels, a connected DualSense Edge Wireless Controller, local Player 2 fight presentation, and P1/P2 hit logs. Detailed Xbox, Start/back/cancel, and full per-button controller matrices remain future checks. | Input runtime feature spec is replaced. |
| `[VERIFIED-LIVE]` | Scripted internal input spine | `--verify kfm-baseline` proves symbolic input reaches FighterInputState, movement, crouch, jump, CMD/CNS normal routing, hit contact, damage, hitpause, and clean scenario exit for KFM. This verifies the internal runtime path, not physical keyboard/controller hardware mapping. | Input/runtime verification architecture changes. |
| `[VERIFIED-LIVE]` | Physical keyboard P1 input | Manual physical keyboard testing confirms KFM held walk, crouch, jump, standing normals, crouching normals, hit/damage, reset, F1/F2, Escape routing, run forward, and dash backward. The same smoke was rerun after input boundary extraction and still works. | Input runtime feature spec is replaced. |
| `[VERIFIED-LIVE]` | Physical keyboard P2 input | Manual physical keyboard smoke confirms P2 keyboard works in a real P2 mode after input boundary extraction. Detailed per-button/facing/player-push matrix can be expanded later. | Input runtime feature spec is replaced. |
| `[VERIFIED-LIVE]` | Controller gamepad smoke | User live testing confirms a DualSense/PlayStation controller works in the running app. Evidence includes Options screen PlayStation pad labels, console detection of `DualSense Edge Wireless Controller (PlayStation)`, VS fight HUD with PlayStation button labels, and P1/P2 hit logs in an Evil Ryu local Player 2 fight. This is a controller smoke pass, not a complete Xbox/PlayStation per-button certification. | Input runtime feature spec is replaced. |
| `[VERIFIED-STATIC]` | SFF/ACT/AIR/SND decode | Current runtime loads SFF v1 PCX sprites, ACT palettes, AIR frames/collision boxes, and SND samples for the active compatibility roster. | Decoder feature spec is replaced. |
| `[PARTIAL]` | CMD routing | Commands feed a buffered command system and State -1 routing rather than hardcoded character buttons. KFM scripted verification live-proved normal attack routing to states `210` and `410`; manual physical keyboard testing also confirmed KFM standing and crouching normals. Controller smoke now shows P1/P2 Evil Ryu hit logs, but full special/super command routing remains unverified. | Command runtime feature spec is replaced. |
| `[VERIFIED-STATIC]` | CNS subset | Current runtime supports the documented controller/trigger subset in `docs/COMPATIBILITY_AUDIT.md`. | CNS runtime feature spec is replaced. |
| `[PARTIAL]` | Hit/guard/fall basics | KFM scripted verification live-proved `HitDef` contact, damage, hitpause, and hit sound on a standing normal. Manual P1 smoke confirmed hitpause/spark/sound feedback after input boundary extraction. Guard, fall/recovery, guard spark/sound, KO, and broader character compatibility remain unverified. | Hit runtime feature spec is replaced. |
| `[VERIFIED-STATIC]` | Compatibility roster | KFM, Evil Ryu, and Evil Ken are the current local compatibility set and must keep load-level checks passing. | The active roster changes in `select.def` and compatibility docs. |
| `[PARTIAL]` | Evil Ken runtime smoke | Screenshot-backed evidence shows Evil Ken can reach fight presentation, display idle/airborne frames, show round intro/timer/combo UI, and render Mountainside Temple/HUD during fight. `--verify evilken-smoke` also live-proved internal scripted load, movement, jump, one normal, timer stability, combo/hit evidence, and clean exit. Specials, supers, nonzero damage, and full compatibility remain unverified. | Deterministic or manual Evil Ken compatibility verification upgrades this. |
| `[VERIFIED-STATIC]` | Benchmark chars | `DragonBench`, `A.Ben`, and `I.Chie` are reserved original benchmark character folders and are not selectable until real M.U.G.E.N files exist. | They become real documented content. |

## Current Non-Goals

- Do not hardcode individual downloaded characters.
- Do not replace M.U.G.E.N backend files with Dragon-only sidecars.
- Do not introduce `.dragon/*.json` runtime sidecars or migrate `.dragon.def` sidecars to JSON.
- Do not add shop, equipment, tournament, storyboards, networking, or editor work before the compatibility play loop is stable.
