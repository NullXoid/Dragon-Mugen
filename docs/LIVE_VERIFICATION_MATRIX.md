# Live Verification Matrix

This matrix records observed runtime behavior. Static audits and parser checks do not count as live verification.

Statuses:

- `PASS`
- `FAIL`
- `PARTIAL`
- `BLOCKED`
- `NOT TESTED`

Current pass metadata:

- Date/time started: `2026-06-04 13:12:01 -04:00`
- Stable commit before Playable Core Proof work: `cf93a57`
- Verification build: current worktree after Playable Core Proof changes
- Launch command: `build/dragon_mugen.exe` from the build folder/no-argument launch path
- Verification commands: `build/dragon_mugen.exe --verify kfm-baseline`, `build/dragon_mugen.exe --verify evilken-smoke`
- Baseline character: `Kung Fu Man`
- Baseline stage: `Mountainside Temple`
- Input extraction checkpoint: App.cpp input boundary extraction manually smoked after build; `App.cpp` line count changed from `16820` to `16724`.
- Training overlay extraction checkpoint: render-only overlay extraction GUI-smoked after build; file-size guard count for `engine/src/App.cpp` changed from `16724` to `16330`. Computer Use can verify event-style GUI behavior, but it cannot perform a true human held-key smoke.
- Pause menu extraction checkpoint: render-only pause menu extraction built and scripted-verifier checked after build; file-size guard count for `engine/src/App.cpp` changed from `16330` to `16288`. Prior Computer Use navigation did not reach Single Player/VS, but user-supplied screenshots on 2026-06-04 confirm Single Player pause rendering and Resume behavior. Later user note: click/focus the game window before sending keyboard input through automation.
- Main menu extraction checkpoint: render-only main/title menu extraction GUI-smoked after build; file-size guard count for `engine/src/App.cpp` changed from `16288` to `16225`. This pass preserved menu rendering and Training route behavior without changing input, routing, settings, fight runtime, or sidecar policy.
- Options menu extraction checkpoint: render-only Options display helpers extracted; file-size guard count for `engine/src/App.cpp` changed from `16225` to `16141`. Prior Computer Use navigation did not reach Options, but user-supplied screenshot evidence on 2026-06-04 confirms the Options screen renders after extraction. Later user note: click/focus the game window before sending keyboard input through automation.
- Controller/P2 evidence checkpoint: user-supplied screenshots and console output on 2026-06-04 confirm a DualSense Edge Wireless Controller was detected, PlayStation pad labels rendered, a VS/local Player 2 fight loaded as Evil Ryu vs Player 2, and P1/P2 hit logs were produced. Detailed Xbox, Start/back/cancel, and full per-button certification remain separate future checks.
- Stage Select extraction checkpoint: render-only Stage Select drawing extracted; file-size guard count for `engine/src/App.cpp` changed from `16141` to `16088`. GUI smoke clicked/focused the game window before input, then verified Stage Select rendering, Escape to Character Select, Enter to fight, Training overlays, reset, backout, and clean exit.
- VS/loading extraction checkpoint: render-only VS/loading drawing extracted; file-size guard count for `engine/src/App.cpp` changed from `16088` to `16030`. GUI smoke clicked/focused the game window before input, captured the Training VS/loading screen, verified transition to fight, Training overlays, reset, backout, and clean exit.
- Character Select extraction checkpoint: render-only Character Select drawing extracted; file-size guard count for `engine/src/App.cpp` changed from `16030` to `15936`. GUI smoke clicked/focused the game window before input, verified Character Select rendering, Enter to Stage Select, Escape from Stage Select back to Character Select, Stage Select -> VS/fight transition, Training overlays, reset, backout, and clean exit. Computer Use arrow-key input did not move the Character Select cursor, so cursor movement and invalid-slot rejection were not recorded as PASS in this pass.
- Fight HUD extraction checkpoint: render-only Fight HUD helpers and `drawFightHud` extracted; file-size guard count for `engine/src/App.cpp` changed from `15936` to `15732`. GUI smoke clicked/focused the game window before input, verified Training fight HUD names/lifebars/power gauges/POWER labels/bottom status text, existing command/input HUD, F1, F2, `R`, one visible normal attack, backout, and clean exit. Timer/round pips were not separately live-verified in this Training-only smoke.
- App state type prep checkpoint: safe app/UI constants, enums, and pure-data structs extracted to `engine/src/AppTypes.h`; file-size guard count for `engine/src/App.cpp` changed from `15598` to `15456`. GUI smoke clicked/focused the game window before input, verified Main -> Training -> KFM -> Mountainside Temple -> VS -> fight, Fight HUD, command/input HUD, F1, F2, `R`, one normal input smoke, Escape backout, and clean exit. No behavior/input/routing/loading/gameplay/controller/CMake/sidecar changes were made.
- Frontend menu decision boundary checkpoint: pure non-fight frontend decision helpers extracted to normal module `engine/src/FrontendMenu.h` / `engine/src/FrontendMenu.cpp`; file-size guard count for `engine/src/App.cpp` changed from `15456` to `15325`. Build, `dev_check`, `kfm-baseline`, and `evilken-smoke` passed. GUI smoke clicked/focused the game window and verified Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, Fight HUD, command/input HUD, F1, F2, `R`, Escape backout, and clean exit. Computer Use delivered Return/Escape/F1/F2/R but did not visibly deliver SDL arrow or letter-key input, so menu selector movement, Options navigation, character/stage cursor movement, and normal-input GUI smoke are marked automation-blocked for this pass.
- Frontend menu decision manual evidence: user manual testing after `4581704` confirmed the keyboard and controller frontend/menu smoke works. Screenshot evidence shows Character Select cursor movement to Evil Ken and Evil Ryu, Stage Select, VS/loading, Training fight, F2 Training Options, F1 hitboxes, and hit/normal evidence. The manual pass also found two air-state bugs now recorded in `docs/BUGS.md`: held diagonal jumps can continue indefinitely, and air attacks can leave the fighter stuck at airborne height.
- Air-state fix checkpoint: focused runtime fix added `--verify kfm-air-state`. The scenario passes `diagonal_jump_forward_lands`, `diagonal_jump_back_lands`, `diagonal_jump_forward_from_walk_lands`, `diagonal_jump_back_from_walk_lands`, and `air_attack_lands` with grounded final snapshots. Existing `--verify kfm-baseline` remains `pass=11 partial=1 fail=0 blocked=0`, and `--verify evilken-smoke` remains `pass=7 partial=0 fail=0 blocked=0`. Physical keyboard/controller retest is still useful but not claimed by automation in this checkpoint.
- Selection state boundary checkpoint: character/stage selection metadata extracted to normal module `engine/src/SelectionState.h` / `engine/src/SelectionState.cpp`; file-size guard count for `engine/src/App.cpp` changed from `15360` to `15272`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke clicked/focused the game window and verified Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, Fight HUD, command/input HUD, F1, F2, `R`, one normal input smoke, Escape backout, and clean exit. Computer Use arrow-key cursor movement remained automation-blocked.
- Frontend state boundary checkpoint: UI/front-end state storage extracted to normal header `engine/src/FrontendState.h`; file-size guard count for `engine/src/App.cpp` changed from `15272` to `15266`, and `FrontendState.h` is 14 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke clicked/focused the game window and verified Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, Fight HUD, command/input HUD, F1, F2, `R`, one normal input smoke, air-state spot check, Escape backout, and clean exit. Routing/loading/runtime behavior and the open passive CPU bug remained unchanged in `App.cpp`.
- Frontend key flow checkpoint: existing key-flow dispatcher functions extracted to transitional internal header `engine/src/FrontendFlow.h`; file-size guard count for `engine/src/App.cpp` changed from `15266` to `14868`, and `FrontendFlow.h` is 405 file-size-guard lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke clicked/focused the game window and verified Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, Fight HUD, command/input HUD, F1, F2, `R`, one normal input smoke, Escape backout, and clean exit. Computer Use still did not move the main selector into Options, so Options/selector movement remains automation-blocked for this pass.
- Training Options behavior checkpoint: pure Training Options behavior helpers extracted to normal module `engine/src/TrainingOptionsBehavior.h` / `engine/src/TrainingOptionsBehavior.cpp`; file-size guard count for `engine/src/App.cpp` changed from `14868` to `14647`, `TrainingOptionsBehavior.h` is 23 lines, and `TrainingOptionsBehavior.cpp` is 231 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke clicked/focused the game window and verified Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight, F2 Training Options labels/statuses, Hitboxes toggle and restore, F2 close, F1 hitboxes, `R`, Escape backout, and clean exit. CPU passive behavior remains open and was not touched.
- Shared UI render helpers checkpoint: shared render-only UI helpers extracted to transitional internal headers `engine/src/UiRenderPrimitives.h` and `engine/src/UiRenderHelpers.h`; file-size guard count for `engine/src/App.cpp` changed from `14647` to `14459`, `UiRenderPrimitives.h` is 110 lines, and `UiRenderHelpers.h` is 94 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. GUI smoke clicked/focused the game window and verified main/title background, Character Select background, roster icons, fixed Dummy slot, Stage Select, VS/loading, fight HUD, command/input HUD, F1, F2, `R`, one normal input, jump spot-check, Escape backout, and clean exit. CPU passive behavior remains open and was not touched.
- CPU baseline checkpoint: focused runtime bug fix added deterministic CPU input generation through `FighterInputState` and the existing `updateControlledFighter` / command / CMD/CNS path, then follow-up P2 hit resolution allowed non-dummy P2 attacks to damage P1. `App.cpp` changed from `14459` to `14494` file-size-guard lines, and `VerificationScenario.cpp` changed from `466` to `597` lines with strengthened `cpu-baseline`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `cpu-baseline` verified Single Player fight phase, P2 movement toward P1, P2 attack state/action `200`, CPU P2 damage against P1, P1 hit/damage against P2, timer stability, and clean exit. This is baseline CPU behavior, not advanced AI or Single Player hitbox-debug display coverage.
- Training state boundary checkpoint: Training Options storage extracted to normal header `engine/src/TrainingState.h`; file-size guard count for `engine/src/App.cpp` changed from `14494` to `14495`, and `TrainingState.h` is 11 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `lastHitText` and `lastHitTextTicks` intentionally stayed out of `TrainingState` during Pass 19 and were subsequently moved to `FightMessageState`. User live retest after `4e80ad7` confirmed the Training route, F2 Training Options labels/statuses, F1, `R`, one normal input, diagonal jump landing, air attack landing, Escape backout, clean exit, CPU baseline behavior, and match-result controller D-pad navigation still work.
- Fight message state boundary checkpoint: shared fight-message storage extracted to normal header `engine/src/FightMessageState.h`; file-size guard count for `engine/src/App.cpp` stayed `14495`, and `FightMessageState.h` is 12 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `RuntimeSnapshot::lastHitText` still exposes verifier hit-message evidence through `state.messages.lastHitText`. Manual GUI smoke was not rerun in this pass.
- Fight display state boundary checkpoint: env-shake display storage and combo counter storage extracted to normal header `engine/src/FightDisplayState.h`; file-size guard count for `engine/src/App.cpp` changed from `14495` to `14490`, and `FightDisplayState.h` is 19 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `RuntimeSnapshot::comboHits` still exposes combo evidence through `state.display.comboCounters`. Env-shake trigger/update behavior, combo mutation functions, camera, global pause, palette/env color, match/round state, hit/damage, rendering, CPU behavior, loading, runtime, and sidecar policy stayed unchanged. Manual GUI smoke was not rerun in this pass.
- UI render primitive seam checkpoint: AppState-free UI primitives moved into normal `engine/src/UiRenderPrimitives.h/.cpp`, and `engine/src/UiRenderContext.h` now provides the resource-free render context used by `ScopedUiScale`. File-size guard count for `engine/src/App.cpp` changed from `14490` to `14503` because this pass adds thin AppState bridge helpers while removing hidden primitive coupling from the transitional header. `UiRenderContext.h` is 10 lines, `UiRenderPrimitives.h` is 31 lines, and `UiRenderPrimitives.cpp` is 87 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `UiRenderHelpers.h`, sprite helpers, resource ownership, overlay headers, gameplay, loading, CPU behavior, controller behavior, and sidecar policy stayed unchanged. Manual GUI smoke was not rerun in this pass.
- Pause menu normal module checkpoint: `PauseMenuOverlay.h` converted to a normal declaration header and `PauseMenuOverlay.cpp` now owns render-only pause menu drawing through `PauseMenuView`, `UiRenderContext`, and public UI primitives. File-size guard count for `engine/src/App.cpp` changed from `14503` to `14507`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Manual GUI smoke was not rerun in this pass.
- Main menu normal module checkpoint: `MainMenuOverlay.h` converted to a normal declaration header and `MainMenuOverlay.cpp` now owns AppState-free foreground menu drawing through `MainMenuView`, `UiRenderContext`, and public UI primitives. File-size guard count for `engine/src/App.cpp` changed from `14507` to `14518`. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Title background/logo resource drawing stayed in App.cpp. Manual GUI smoke was not rerun in this pass.
- Stage Select normal module checkpoint: `StageSelectOverlay.h` converted to a normal declaration header and `StageSelectOverlay.cpp` now owns AppState-free Stage Select foreground drawing through `StageSelectView`, `StageSelectRowView`, `UiRenderContext`, and public UI primitives. File-size guard count for `engine/src/App.cpp` changed from `14518` to `14543` because App.cpp now explicitly assembles prepared view data. `StageSelectOverlay.h` is 27 lines and `StageSelectOverlay.cpp` is 67 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Selection metadata lookup, routing, loading, resource ownership, gameplay, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- Options menu normal module checkpoint: `OptionsMenuOverlay.h` converted to a normal declaration header and `OptionsMenuOverlay.cpp` now owns AppState-free Options foreground drawing through `OptionsMenuView`, `OptionsMenuRowView`, `UiRenderContext`, and public UI primitives. File-size guard count for `engine/src/App.cpp` changed from `14543` to `14610` because App.cpp now explicitly assembles prepared settings rows and pad summary text. `OptionsMenuOverlay.h` is 24 lines and `OptionsMenuOverlay.cpp` is 59 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Settings behavior, gamepad status ownership, title background resources, routing, gameplay, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- VS/loading normal module checkpoint: `VsScreenOverlay.h` converted to a normal declaration header and `VsScreenOverlay.cpp` now owns AppState-free VS/loading presentation through `VsScreenView`, `UiRenderContext`, and public UI primitives. File-size guard count for `engine/src/App.cpp` changed from `14610` to `14649` because App.cpp now explicitly assembles selected labels, load status, and a non-owning portrait view. `VsScreenOverlay.h` is 35 lines and `VsScreenOverlay.cpp` is 115 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Portrait/resource ownership, loading, routing, gameplay, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- UI sprite view seam checkpoint: `UiSpriteView.h/.cpp` now provides a public non-owning sprite view and top-left sprite drawing helper. `VsScreenOverlay` now uses `UiSpriteView` instead of a private portrait view. File-size guard count for `engine/src/App.cpp` changed from `14649` to `14652`; `UiSpriteView.h` is 25 lines, `UiSpriteView.cpp` is 32 lines, `VsScreenOverlay.h` is 28 lines, and `VsScreenOverlay.cpp` is 101 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Texture/resource ownership, loading, routing, gameplay, controller behavior, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- Character Select normal module checkpoint: `CharacterSelectOverlay.h` converted to a normal declaration header and `CharacterSelectOverlay.cpp` now owns AppState-free Character Select foreground drawing through `CharacterSelectView`, `CharacterCellView`, `UiRenderContext`, public UI primitives, and `UiSpriteView`. File-size guard count for `engine/src/App.cpp` changed from `14652` to `14704` because App.cpp now explicitly assembles selected/page data and non-owning sprite views. `CharacterSelectOverlay.h` is 32 lines and `CharacterSelectOverlay.cpp` is 132 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Roster state, selection state, sprite/resource lookup, select-background drawing, routing, loading, gameplay, controller behavior, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- Fight presentation view seam checkpoint: `FightPresentationView.h` now provides display-only prepared view structs for future Fight HUD/result module conversion. File-size guard count for `engine/src/App.cpp` changed from `14704` to `14705`, and `FightPresentationView.h` is 95 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. No fight overlay call sites, rendering behavior, runtime logic, gameplay, loading, resources, CPU behavior, controller behavior, or sidecar policy changed. Manual GUI smoke was not rerun in this seam-only pass.
- Fight HUD normal module checkpoint: `FightHudOverlay.h` converted to a normal declaration header and `FightHudOverlay.cpp` now owns render-only Fight HUD drawing from prepared `FightHudView` data. File-size guard count for `engine/src/App.cpp` changed from `14705` to `14795` because App.cpp now explicitly assembles runtime-derived HUD view data. `FightHudOverlay.h` is 10 lines and `FightHudOverlay.cpp` is 197 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Hit/damage, combo mutation, life/power/timer mutation, round flow, CMD/CNS, training behavior, CPU behavior, loading, audio, resources, controller behavior, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass.
- Fight Result normal module checkpoint: `FightResultOverlay.h` converted to a normal declaration header and `FightResultOverlay.cpp` now owns render-only round/result/match-result drawing from prepared `FightPresentationView` data. File-size guard count for `engine/src/App.cpp` changed from `14795` to `14886` because App.cpp now explicitly assembles runtime-derived result view data. `FightResultOverlay.h` is 13 lines and `FightResultOverlay.cpp` is 133 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Round-flow decisions, result semantics, result-menu mutation/routing, hit/damage, CMD/CNS, CPU behavior, loading, audio, resources, controller behavior, and sidecar policy stayed in App.cpp/existing modules. Manual GUI smoke and dedicated result-screen smoke were not rerun in this pass.
- Training Options normal module checkpoint: `TrainingOptionsOverlay.h` converted to a normal declaration header and `TrainingOptionsOverlay.cpp` now owns render-only F2 Training Options and command move-list drawing from prepared `TrainingOptionsMenuView` and `TrainingMoveListView` data. File-size guard count for `engine/src/App.cpp` changed from `14886` to `14975` because App.cpp now explicitly assembles training options and move-list display data. `TrainingOptionsOverlay.h` is 48 lines and `TrainingOptionsOverlay.cpp` is 138 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Training option mutation, F1/F2/R dispatch, command matching, CMD/CNS, dummy behavior, hit/damage, input, loading, resources, controller behavior, and sidecar policy stayed in App.cpp/existing modules. Manual F2/move-list GUI smoke was not rerun in this pass.
- Training Command HUD view seam checkpoint: `TrainingCommandView.h` created a display-only command/input HUD seam with `TrainingCommandRowView`, `TrainingInputHudView`, and `TrainingCommandHudView`. File-size guard count for `engine/src/App.cpp` changed from `14975` to `14976` because App.cpp only compile-checks the header. `TrainingCommandView.h` is 27 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `TrainingCommandOverlay.h` remains transitional; command matching, input history reading, `FighterState`, `CommandStateEntry`, CMD/CNS data, training runtime behavior, input handling, hit/damage, loading, resources, controller behavior, sidecar policy, and `.dragon/*.json` stayed unchanged. Manual GUI smoke was not rerun in this seam-only pass.
- Training Command normal module checkpoint: `TrainingCommandOverlay.h` converted to a normal declaration header and `TrainingCommandOverlay.cpp` now owns render-only command/input HUD drawing from prepared `TrainingCommandHudView` data. File-size guard count for `engine/src/App.cpp` changed from `14976` to `15035` because App.cpp now explicitly assembles command/input HUD display data. `TrainingCommandOverlay.h` is 10 lines and `TrainingCommandOverlay.cpp` is 72 lines. Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. Command matching, command-list filtering semantics, input history ownership, `FighterState`, `CommandStateEntry`, CMD/CNS data, training runtime behavior, input handling, hit/damage, loading, resources, controller behavior, sidecar policy, and `.dragon/*.json` stayed unchanged. Manual command/input HUD GUI smoke was not rerun in this pass.

| Area | Character | Mode | Input / Action | Expected Result | Actual Result | Status | Evidence / Notes |
|---|---|---|---|---|---|---|---|
| App shell | N/A | Main | Launch `build/dragon_mugen.exe` with no game argument | App auto-resolves the runtime root and opens | Dragon MUGEN window opened with full M.U.G.E.N title art | PASS | 2026-06-04, commit `e0bc149`, launched through Computer Use with the build executable path. |
| App shell | N/A | Main | Main/title screen | M.U.G.E.N-style title/main menu appears | Main screen showed M.U.G.E.N motif, `DRAGON MUGEN CORE`, and Training highlighted | PASS | Screenshot captured through Computer Use. |
| Main menu extraction | N/A | Main | Post-extraction main/title render | Render-only extraction preserves title screen presentation | Main screen showed M.U.G.E.N title art, `DRAGON MUGEN CORE`, menu frame, Training highlight, mode description, and footer/help text | PASS | 2026-06-04 current worktree after `MainMenuOverlay.h` extraction; launched `build/dragon_mugen.exe` through Computer Use. |
| Main menu normal module conversion | N/A | Static/architecture | Convert foreground `MainMenuOverlay` to normal module | Foreground menu should render from `MainMenuView` and public UI primitives without App.cpp-local types | `MainMenuOverlay.h` declares `MainMenuView`; `MainMenuOverlay.cpp` owns foreground menu drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 25. Title background/logo resource drawing stayed in App.cpp. Manual GUI smoke was not rerun in this pass. |
| Stage Select normal module conversion | N/A | Static/architecture | Convert foreground `StageSelectOverlay` to normal module | Stage Select should render from prepared view data and public UI primitives without App.cpp-local types | `StageSelectOverlay.h` declares `StageSelectView` and `StageSelectRowView`; `StageSelectOverlay.cpp` owns foreground drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 26. Selection semantics, routing, loading, and resource ownership stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass. |
| Options menu normal module conversion | N/A | Static/architecture | Convert foreground `OptionsMenuOverlay` to normal module | Options should render from prepared rows and public UI primitives without App.cpp-local types | `OptionsMenuOverlay.h` declares `OptionsMenuView` and `OptionsMenuRowView`; `OptionsMenuOverlay.cpp` owns foreground drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 27. Settings mutation, gamepad ownership/status assembly, routing, and title-background resources stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass. |
| VS/loading normal module conversion | N/A | Static/architecture | Convert `VsScreenOverlay` to normal module | VS/loading should render from prepared labels and a non-owning portrait view without App.cpp-local types | `VsScreenOverlay.h` declares `VsScreenView`; `VsScreenOverlay.cpp` owns VS/loading presentation; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 28. Portrait/resource ownership, selected metadata assembly, loading, routing, and fight preparation stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass. |
| UI sprite view seam | N/A | Static/architecture | Create public non-owning sprite view for overlay modules | Normal overlays should be able to receive sprite handles without taking `TextureSprite` or resource ownership | `UiSpriteView.h/.cpp` declares a non-owning sprite handle and top-left draw helper; `VsScreenOverlay` now uses it; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 29. `TextureSprite`, caches, loading, and ownership stayed in App.cpp. Manual GUI smoke was not rerun in this pass. |
| Character Select normal module conversion | N/A | Static/architecture | Convert foreground `CharacterSelectOverlay` to normal module | Character Select should render from prepared view data and public sprite/render seams without App.cpp-local types | `CharacterSelectOverlay.h` declares `CharacterSelectView` and `CharacterCellView`; `CharacterSelectOverlay.cpp` owns foreground drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 30. Roster semantics, sprite/resource lookup, select-background drawing, routing, loading, and resource ownership stayed in App.cpp/existing modules. Manual GUI smoke was not rerun in this pass. |
| Fight presentation view seam | N/A | Static/architecture | Create prepared fight presentation view structs | Fight HUD/result modules should have display-only view structs before normal module conversion | `FightPresentationView.h` declares display-only fight HUD/result view structs and is compile-checked from `App.cpp`; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 32. `FightHudOverlay` and `FightResultOverlay` now use the seam as normal modules; `FightPresentationShared` remains transitional. Manual GUI smoke was not rerun in this pass. |
| Fight HUD normal module conversion | N/A | Static/architecture | Convert `FightHudOverlay` to normal module | Fight HUD should render from prepared `FightHudView` without App.cpp-local types in the module | `FightHudOverlay.h` declares `drawFightHud(const UiRenderContext&, const FightHudView&)`; `FightHudOverlay.cpp` owns render-only HUD drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 33. `App.cpp` assembles all runtime-derived HUD data. No manual GUI smoke was rerun, so timer/round pips and combo counter live PASS claims remain separate. |
| Fight Result normal module conversion | N/A | Static/architecture | Convert `FightResultOverlay` to normal module | Result overlays should render from prepared result views without App.cpp-local types in the module | `FightResultOverlay.h` declares result draw functions using `UiRenderContext` and `FightPresentationView` structs; `FightResultOverlay.cpp` owns render-only result drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 34. `App.cpp` assembles all runtime-derived result data. No manual result-screen GUI smoke was rerun, so round/result live PASS claims remain separate. |
| Training Options normal module conversion | N/A | Static/architecture | Convert `TrainingOptionsOverlay` to normal module | F2 options and move-list pages should render from prepared views without App.cpp-local types in the module | `TrainingOptionsOverlay.h` declares `TrainingOptionsMenuView` and `TrainingMoveListView`; `TrainingOptionsOverlay.cpp` owns render-only options/move-list drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 36. `App.cpp` assembles all training option and command move-list display data. No manual F2/move-list GUI smoke was rerun. |
| Training Command HUD view seam | N/A | Static/architecture | Create command/input HUD view structs before overlay conversion | Future command/input HUD module should receive prepared strings and flags without runtime types | `TrainingCommandView.h` declares `TrainingCommandRowView`, `TrainingInputHudView`, and `TrainingCommandHudView`; App.cpp compile-checks the header; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 37. No render call sites changed, and manual GUI smoke was not rerun. |
| Training Command normal module conversion | N/A | Static/architecture | Convert `TrainingCommandOverlay` to normal module | Command/input HUD should render from prepared views without App.cpp-local types in the module | `TrainingCommandOverlay.h` declares `drawTrainingCommandOverlay`; `TrainingCommandOverlay.cpp` owns render-only command/input HUD drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 38. `App.cpp` assembles all command/input HUD display data. No manual command/input HUD GUI smoke was rerun. |
| Main menu extraction | Kung Fu Man | Training | Post-extraction Training route smoke | Main menu extraction does not break Training route or existing overlays | Training -> character select -> stage select -> VS -> fight worked; command/input HUD, visible normal-attack effect, F1 hitboxes, F2 options, `R`, Escape backout, and clean exit were observed | PASS | GUI smoke after main menu extraction. This is event-style automation evidence; held-input status remains based on scripted verification and prior user manual evidence. |
| App shell | N/A | Main | Settings screen | Settings menu opens | User manually navigated to Options and provided screenshot evidence showing the Options screen | PASS | 2026-06-04 manual user evidence after Options menu extraction. Prior automation did not navigate there; user later clarified the game window must be focused before automation keyboard input registers. |
| Options menu extraction | N/A | Options | Post-extraction Options render | Options screen renders current rows, values, and selected-row highlight | Screenshot shows `OPTIONS`, selected `MATCH TIMER`, values for timer, canvas size, UI scale, pad labels, P1/P2 gamepad rows, and `BACK` | PASS | User-supplied screenshot evidence on 2026-06-04 after `OptionsMenuOverlay.h` extraction. This verifies rendering, not controller hardware. |
| App shell | Kung Fu Man | Training | Character select | Character select opens and shows roster icons | KFM, Evil Ryu, and Evil Ken portraits/icons visible | PASS | Enter from highlighted Training opened character select. |
| Character Select extraction | Kung Fu Man | Training | Post-extraction Character Select render | Character Select renders title, visible roster icons, selected portrait/name, opponent panel, cursor/highlight, stage label, and footer | Character Select rendered `TRAINING`, `SELECT YOUR FIGHTER`, KFM/Evil Ryu/Evil Ken icons, KFM portrait/name, Dummy panel, P1 cursor/highlight, `STAGE: Mountainside Temple`, and footer after `CharacterSelectOverlay.h` extraction | PASS | 2026-06-04 23:05 -04:00 GUI smoke after build; game window was clicked/focused before keyboard input. |
| Character Select extraction | Kung Fu Man | Training | Cursor movement Right/Left | Right moves to another valid slot and Left returns to KFM | Computer Use `Right` and `d` key attempts did not move the Character Select cursor, while Enter/Escape routing still worked | BLOCKED | Automation limitation only; no runtime failure recorded. Do not count this as physical keyboard cursor verification. |
| Character Select extraction | Kung Fu Man | Training | Invalid-slot rejection | Cursor should not land on invalid/empty slots | Not attempted because automation could not move the Character Select cursor | NOT TESTED | Leave for manual or improved automation verification. |
| Character Select extraction | Kung Fu Man | Training | Stage/fight route | Enter still proceeds to Stage Select and then VS/fight | Enter from Character Select reached Stage Select; Escape from Stage Select returned to Character Select; Enter through Stage Select and VS/loading reached KFM vs Dummy on Mountainside Temple | PASS | GUI smoke after Character Select extraction. |
| Character Select extraction | Kung Fu Man | Training | Post-extraction Training overlay smoke | Character Select extraction does not affect Training fight overlays or backout | Fight showed command/input HUD; F1 hitboxes toggled; F2 Training Options opened; `R` reset did not crash; Escape routed out and closed the app | PASS | GUI smoke after Character Select extraction. |
| App shell | Kung Fu Man | Training | Stage select | Stage select opens | Stage Select showed Mountainside Temple | PASS | Enter from character select. |
| Stage Select extraction | Kung Fu Man | Training | Post-extraction Stage Select render | Stage Select renders title, arena counter, list, selected highlight, details, fighter/opponent labels, and footer | Stage Select rendered `DRAGON MUGEN CORE`, `STAGE SELECT`, `2/2 ARENA`, selected `Mountainside Temple`, id/author/fighter/opponent details, and footer text after `StageSelectOverlay.h` extraction | PASS | 2026-06-04 GUI smoke after build; game window was clicked/focused before keyboard input. |
| Stage Select extraction | Kung Fu Man | Training | Escape from Stage Select | Esc returns to Character Select without changing stage-select behavior | Escape from Stage Select returned to the Training Character Select screen | PASS | GUI smoke after Stage Select extraction. |
| Stage Select extraction | Kung Fu Man | Training | Enter from Stage Select | Enter proceeds through VS/fight using the selected stage | Enter from Stage Select reached the Training fight view on Mountainside Temple with KFM vs Dummy | PASS | GUI smoke after Stage Select extraction. |
| Stage Select extraction | Kung Fu Man | Training | Post-extraction Training overlay smoke | Stage Select extraction does not affect Training fight overlays or backout | Fight showed command/input HUD; F1 hitboxes toggled; F2 Training Options opened; `R` reset did not crash; Escape routed fight -> Stage Select -> Character Select -> Main -> closed app | PASS | GUI smoke after Stage Select extraction. |
| App shell | Kung Fu Man | Training | VS screen | VS screen opens before fight | Training VS screen showed KFM, Dummy, and Mountainside Temple | PASS | Enter from stage select. |
| VS/loading extraction | Kung Fu Man | Training | Post-extraction VS/loading render | VS/loading screen renders mode title, panels, portraits/placeholders, names, VS label, stage line, and loading/ready text | Quick snapshot after Stage Select Enter showed `TRAINING VS`, KFM portrait/name, Dummy placeholder/name, `VS`, `Stage: Mountainside Temple`, `Ready. ENTER start now`, and `Auto-start after a short pause` | PASS | 2026-06-04 GUI smoke after `VsScreenOverlay.h` extraction; no timing changes were introduced. |
| VS/loading extraction | Kung Fu Man | Training | Auto-transition to fight | VS/loading screen still transitions to fight | After the existing short pause, fight loaded KFM vs Dummy on Mountainside Temple with command/input HUD | PASS | GUI smoke after VS/loading extraction. |
| VS/loading extraction | Kung Fu Man | Training | Post-extraction Training overlay smoke | VS/loading extraction does not affect Training fight overlays or backout | F1 hitboxes toggled, F2 Training Options opened, `R` reset did not crash, and Escape routed out to closed app | PASS | GUI smoke after VS/loading extraction. |
| Fight HUD extraction | Kung Fu Man | Training | Post-extraction Fight HUD render | Fight HUD renders P1/P2 names, lifebars, power gauges, POWER labels, and bottom status/help text | Training fight displayed `Kung Fu Man`, `Dummy`, green lifebars, power gauges with `POWER` labels, and bottom status/help text after `FightHudOverlay.h` extraction | PASS | 2026-06-05 08:54 -04:00 GUI smoke after build; game window was clicked/focused before keyboard input. |
| Fight HUD extraction | Kung Fu Man | Training | Existing Training HUD regression check | Training command/input HUD remains separate and still renders | Command/input HUD still rendered over the fight after Fight HUD extraction | PASS | Confirms the prior Training overlay extraction was not regressed; command/input HUD code was not moved in this pass. |
| Fight HUD extraction | Kung Fu Man | Training | Training controls after HUD extraction | F1/F2/R/attack/backout remain unaffected | F1 hitboxes toggled, F2 Training Options opened, `R` reset did not crash, one normal attack produced a visible effect, and Escape closed the app cleanly | PASS | GUI smoke after Fight HUD extraction. |
| Fight HUD extraction | Kung Fu Man | Single Player/VS | Timer and round pips after HUD extraction | Match HUD timer and round pips should render in match modes | Not attempted in this pass | NOT TESTED | Training fight smoke does not separately prove match-mode timer/round pip rendering. |
| Fight result overlay extraction | Kung Fu Man | Training | Post-extraction Training fight smoke | Shared/result overlay extraction preserves the visible Training fight route and prior HUD overlays | Training -> KFM -> Mountainside Temple -> VS -> fight worked; Fight HUD names/lifebars/power gauges/`POWER` labels, command/input HUD, F1 hitboxes, F2 Training Options, `R`, normal-input smoke, Escape backout, and clean exit were observed | PASS | 2026-06-05 GUI smoke after `FightPresentationShared.h` and `FightResultOverlay.h` extraction. Game window was clicked/focused before keyboard input. `App.cpp` count changed 15732 -> 15598. |
| Fight result overlay extraction | Kung Fu Man | Single Player/VS | Round/result overlay screens | Round start/finish/result/match result overlays should render from the extracted result overlay header | Not forced or separately observed in this pass | NOT TESTED | Verifiers remained green, but dedicated KO/round-result/match-result GUI checks are still needed before marking result overlays live PASS. |
| App state type prep extraction | Kung Fu Man | Training | Post-extraction Training route smoke | Moving safe app/UI types to `AppTypes.h` preserves the visible Training route and existing overlays | Main -> Training -> KFM -> Mountainside Temple -> VS -> fight worked; Fight HUD, command/input HUD, F1 hitboxes, F2 Training Options, `R`, normal-input smoke, Escape backout, and clean exit were observed | PASS | 2026-06-05 GUI smoke after `AppTypes.h` extraction. `App.cpp` count changed 15598 -> 15456; `AppTypes.h` is 150 lines. |
| App state type prep extraction | N/A | Static/type prep | Moved definitions | Safe constants, enums, and pure-data structs move without behavior changes | `AppTypes.h` owns moved constants/enums/structs; `AppState`, `FighterState`, runtime/resource-owned types, input, routing, loading, gameplay, CMake, and sidecar policy remained unchanged | PASS | Build, `dev_check`, `--verify kfm-baseline`, and `--verify evilken-smoke` passed after extraction. |
| Frontend menu decision boundary | N/A | Static/architecture | Extract pure frontend decisions | `FrontendMenu` decides requested actions and pure index/settings changes while `App.cpp` keeps side effects | `FrontendMenu.h/.cpp` define `FrontendKey`, `FrontendAction`, main/options/character/stage/VS decision helpers, and copied `MainSettings` cycling; `App.cpp` still handles SDL conversion, routing mutations, loading, fight startup, gamepad ownership, runtime, audio, and sidecar policy | PASS | Build, `dev_check`, `--verify kfm-baseline`, and `--verify evilken-smoke` passed after extraction. `App.cpp` count changed 15456 -> 15325. |
| Frontend menu decision boundary | N/A | Main | Main route after extraction | Main menu still renders and Accept on Training opens the Training route | No-argument launch showed the title/main menu; after clicking/focusing the window, Return opened Training Character Select | PASS | 2026-06-05 GUI smoke after `FrontendMenu` extraction. |
| Frontend menu decision boundary | N/A | Main/Options | Selector movement and Options navigation | Down should move the main selector and allow Options entry | Computer Use `Down`/`Right` style input did not visibly move SDL menu selectors, although Return/Escape worked | BLOCKED | Automation limitation only; do not count as runtime failure or physical keyboard evidence. Manual Options evidence from the prior Options extraction remains valid historically but was not re-smoked after this pass. |
| Frontend menu decision boundary | Kung Fu Man | Training | Character/Stage/VS/fight route | Character Select, Stage Select, VS/loading, and fight route still work | Return from Character Select reached Stage Select; Return from Stage Select reached VS/loading; existing auto-start reached KFM vs Dummy on Mountainside Temple | PASS | GUI smoke after `FrontendMenu` extraction. |
| Frontend menu decision boundary | Kung Fu Man | Training | Character/stage cursor movement | Arrow keys should move character and stage cursors | Computer Use arrow-key attempts did not visibly move the Character Select cursor; stage cursor movement was not separately proven for the same reason | BLOCKED | Automation limitation only; prior manual keyboard/controller evidence remains separate. |
| Frontend menu decision boundary | Kung Fu Man | Training | Training fight regression smoke | Fight HUD, Training overlays, reset, backout, and clean exit remain unaffected | Fight HUD and command/input HUD rendered; F1 hitboxes toggled; F2 Training Options opened; `R` reset did not crash; Escape returned to Stage Select and Escape chain closed the app cleanly | PASS | Letter-key normal input was not visibly delivered by Computer Use in this pass, so normal-input GUI smoke is not marked PASS here; scripted verifier still proves CMD/CNS normals. |
| Frontend menu decision boundary manual smoke | Kung Fu Man / Evil Ryu / Evil Ken | Training / controller smoke | Manual keyboard/controller smoke after extraction | Manual input should exercise the rows Computer Use could not prove | User reported everything tested worked with keyboard and controller. Screenshot evidence shows Character Select cursor movement to Evil Ken and Evil Ryu, Stage Select, VS/loading, Training fight HUD/command HUD, F2 Training Options, F1 hitboxes, and hit/normal evidence. | PASS | Manual evidence after commit `4581704`. This upgrades the automation-blocked frontend rows as physical/manual evidence, while keeping the new air-state bugs separate. |
| Air-state fix | Kung Fu Man | Training / scripted verifier | Hold Up + Forward/Back from neutral or while walking through landing window | Character should perform one jump arc by default, land, and not re-enter air while the same Up input is held | `--verify kfm-air-state` passed forward/back diagonal checks from neutral and from held walk with `saw_air=1`, `landed=1`, `reentered_air_after_landing=0`, `final_y=0.000000`, `final_vy=0.000000`, `final_state=0`, and `final_on_ground=1` | PASS | Scripted internal runtime proof after the focused air-state fix. Physical keyboard/controller retest is still useful but not claimed here. Double jump remains a future explicit setting. |
| Air-state fix | Kung Fu Man | Training / scripted verifier | Jump then perform air attack | Character should continue normal fall/landing and return to stage floor | `--verify kfm-air-state` passed `air_attack_lands` with `saw_air=1`, `saw_air_attack=1`, `landed_after_attack=1`, `final_y=0.000000`, `final_vy=0.000000`, `final_state=0`, and `final_on_ground=1` | PASS | Scripted internal runtime proof after the focused air-state fix; no physical/manual retest is claimed in this row. |
| Selection state boundary | Kung Fu Man | Training | Post-extraction route and selection smoke | Selection metadata split should preserve character/stage route, selected metadata, and fight load | Main -> Training -> Character Select showed KFM/Evil Ryu/Evil Ken; Enter reached Stage Select with Mountainside Temple/KFM/Dummy details; Enter reached VS/loading and then KFM vs Dummy fight; HUD/command HUD, F1, F2, `R`, one normal input smoke, Escape backout, and clean exit worked | PASS | 2026-06-05 GUI smoke after `SelectionState` extraction. `select.def` loading, runtime loading, routing side effects, and sidecar policy were unchanged. Computer Use arrow-key cursor movement remained automation-blocked. |
| Frontend state boundary | Kung Fu Man | Training | Post-extraction frontend route and fight smoke | Moving frontend storage should preserve routing behavior, overlays, and fight entry | Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight worked; HUD/command HUD, F1, F2, `R`, one normal input smoke, air-state spot check, Escape backout, and clean exit worked | PASS | 2026-06-05 GUI smoke after `FrontendState` extraction. `App.cpp` count changed `15272` -> `15266`; routing, screen transition behavior, loading, runtime, and CPU behavior stayed in `App.cpp`. |
| Frontend key flow boundary | N/A | Static/architecture | Extract existing key-flow dispatcher | `FrontendFlow.h` should be transitional and keep using App.cpp-local helpers without moving helper implementations | `frontendKeyFromSdl`, `settingCycleDirection`, `handleKey`, and `gamepadMenuKeyForButton` moved into `FrontendFlow.h`; `pumpEvents`, SDL polling, gamepad open/close, fixed update, render loop, loading/runtime helpers, `AppState`, and `FighterState` stayed in `App.cpp` | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. `App.cpp` count changed `15266` -> `14868`; `FrontendFlow.h` is 405 lines. |
| Frontend key flow boundary | Kung Fu Man | Training | Post-extraction route and fight key smoke | Moving key flow should preserve route, fight overlays, reset, attack, and backout | Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight worked; HUD/command HUD, F1 hitboxes, F2 Training Options, `R`, normal input, Escape backout, and clean exit worked | PASS | 2026-06-05 GUI smoke after `FrontendFlow.h` extraction. Game window was clicked/focused before keyboard input. |
| Frontend key flow boundary | N/A | Main/Options | Main selector movement and Options open/back | Down should move to Options and Enter should open it | Computer Use `Down` input still did not visibly move the SDL main selector; Return opened Training from the default row | BLOCKED | Automation limitation only; do not mark as runtime fail or CPU/input regression. Prior manual keyboard/controller evidence remains separate. |
| Training Options behavior boundary | N/A | Static/architecture | Extract pure Training Options behavior helpers | New module should read/write only `TrainingOptions` and return labels/status text; runtime side effects stay outside it | `dummyGuardModeStatus`, `cycleDummyGuardMode`, `trainingPowerModeStatus`, `cycleTrainingPowerMode`, `trainingMoveCategoryStatus`, `cycleTrainingMoveCategory`, `trainingOptionLabel`, `trainingOptionStatus`, and pure `toggleTrainingOption` moved to `TrainingOptionsBehavior.h/.cpp`; runtime side effects stayed in `App.cpp`/`FrontendFlow.h` | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. `App.cpp` count changed `14868` -> `14647`; new module line counts are 23/231. |
| Training Options behavior boundary | Kung Fu Man | Training | Post-extraction F2 option behavior smoke | Training Options labels/statuses should render; a pure display option should toggle and restore; route/fight controls should remain working | Main -> Training -> Character Select -> Stage Select -> VS/loading -> fight worked; F2 Training Options rendered labels/statuses; Hitboxes toggled ON and restored OFF; F2 closed; F1 hitboxes, `R`, Escape backout, and clean exit worked | PASS | 2026-06-05 GUI smoke after `TrainingOptionsBehavior` extraction. Game window was clicked/focused before keyboard input. Diagonal/air-attack landing remains covered by `kfm-air-state`. |
| Shared UI render helpers boundary | N/A | Static/architecture | Extract render-only UI helpers without resource ownership | Helper headers should move primitive UI drawing and title/select placeholder presentation while leaving sprite/resource ownership and runtime rendering in `App.cpp` | `UiRenderPrimitives.h` owns color/rect/text/scale/panel/fit helpers; `UiRenderHelpers.h` owns title/select backgrounds and fixed opponent slot rendering. Sprite helpers, texture creation, `TextureSprite`, `SystemScreenAssets`, actor/fighter/stage rendering, runtime effect/projectile rendering, loading, audio, and gameplay stayed in `App.cpp` | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, and `kfm-air-state` passed. `App.cpp` count changed `14647` -> `14459`; helper headers are 110 and 94 lines. |
| Shared UI render helpers boundary | Kung Fu Man | Training | Post-extraction UI route smoke | Title/select backgrounds, fixed Dummy slot, fight HUD, and Training overlays should still render | Main/title background rendered; Main -> Training showed Character Select background, roster icons, fixed Dummy slot, labels/footer; Stage Select -> VS/loading -> fight worked; HUD/command HUD, F1, F2, `R`, normal input, jump spot-check, Escape backout, and clean exit worked | PASS | 2026-06-05 GUI smoke after `UiRenderPrimitives.h` / `UiRenderHelpers.h` extraction. Game window was clicked/focused before keyboard input. |
| App shell | Kung Fu Man | Training | Fight view | Fight loads selected character and stage after VS | Fight view loaded KFM, Dummy, Mountainside Temple, power bars, command HUD | PASS | Auto-advance from VS. |
| App shell | Kung Fu Man | Training | Escape/back routing | Back out through fight/stage/character/main and exit | Fight -> Stage Select -> Character Select -> Main -> app closed | PASS | Escape sequence verified; `list_apps` showed no Dragon MUGEN window after final Escape. |
| Training tools | Kung Fu Man | Training | `F1` | Hitbox overlay toggles | Blue hitboxes appeared around KFM and Dummy | PASS | F1 event input verified live on commit `e0bc149`. |
| Training tools | Kung Fu Man | Training | `F2` | Training options opens | Training Options panel opened | PASS | F2 event input verified live on commit `e0bc149`. |
| Training overlay extraction | Kung Fu Man | Training | Post-extraction title/select/fight path | Render-only extraction preserves visual navigation path | Title screen, character select, stage select, VS screen, and fight view were reached after extraction | PASS | 2026-06-04 current worktree after Training overlay extraction; launched `build/dragon_mugen.exe` through Computer Use. |
| Training overlay extraction | Kung Fu Man | Training | Post-extraction command/input HUD | Command/input HUD still renders in fight view | `INPUT` and `COMMANDS` panel rendered over Mountainside Temple | PASS | GUI smoke after extraction. |
| Training overlay extraction | Kung Fu Man | Training | Post-extraction `F1` | Hitbox/debug overlay still toggles | Blue hitboxes appeared around KFM and Dummy | PASS | GUI smoke after extraction. |
| Training overlay extraction | Kung Fu Man | Training | Post-extraction `F2` | Training Options still opens | Training Options panel opened with option columns | PASS | GUI smoke after extraction. |
| Training overlay extraction | Kung Fu Man | Training | Post-extraction `R` and `Esc` | Reset key does not crash; Escape backs out cleanly | `R` was tapped, then Escape routed fight -> Stage Select -> Character Select -> Main -> app closed | PASS | GUI smoke after extraction. This is event-style automation evidence, not a true held-key movement proof. |
| Pause menu extraction | Kung Fu Man | Single Player | Pause menu rendering | Existing pause menu renders with current items and selected-row highlight | User-supplied screenshot shows `SINGLE PLAYER PAUSE`, `RESUME`, `RESTART MATCH`, `FIGHTER SELECT`, `STAGE SELECT`, `MODE SELECT`, selected-row highlight, and footer text | PASS | Image-backed manual evidence on 2026-06-04 after pause-menu extraction. |
| Pause menu extraction | Kung Fu Man | VS Mode | Pause menu rendering | Existing pause menu renders with current items and selected-row highlight | User-supplied screenshot shows `VS MODE PAUSE`, `RESUME`, `RESTART MATCH`, `FIGHTER SELECT`, `STAGE SELECT`, `MODE SELECT`, selected-row highlight, and footer text | PASS | Image-backed manual evidence on 2026-06-04 after pause-menu extraction. |
| Pause menu extraction | Kung Fu Man | Single Player | Resume from pause | Resume closes pause menu and returns to fight | User-supplied screenshot shows fight resumed after pause menu, with timer continuing from `90` to `87` | PASS | Image-backed manual evidence on 2026-06-04. Other pause actions still need individual live checks. |
| Pause menu normal module conversion | N/A | Static/architecture | Convert `PauseMenuOverlay` to normal module | Overlay should render from `PauseMenuView` and public UI primitives without App.cpp-local types | `PauseMenuOverlay.h` declares `PauseMenuView`; `PauseMenuOverlay.cpp` owns render-only drawing; build and scripted verifiers passed | PASS | Static/scripted evidence after Pass 24. Manual pause GUI smoke was not rerun in this pass. |
| Single Player match flow | Evil Ken | Single Player | Play through to match completion | Match shell should support match-complete/result presentation | Screenshot shows `MATCH COMPLETE`, `SINGLE PLAYER`, Evil Ken winning `2 - 0`, `Won by Decision`, stage `Mountainside Temple`, and result-menu rows | PASS | User-supplied screenshot evidence on 2026-06-05. This verifies match flow/result presentation, not CPU fighting behavior. |
| Match result controller navigation | Evil Ken | Single Player | Controller D-pad Up/Down on `MATCH COMPLETE` result menu | D-pad Up/Down should move the result-menu highlight like keyboard Up/Down | User live retest after `4e80ad7` confirmed controller D-pad Up/Down now moves the result-menu highlight, and confirm/cancel buttons still work | PASS | Focused input-routing fix after `2c71808`; upgraded after hardware retest on 2026-06-05. |
| Single Player CPU behavior baseline | KFM / CPU opponent | Single Player | `--verify cpu-baseline` | CPU should move, attack, damage P1, remain hittable, and route through FighterInputState/CMD/CNS/HitDef | P2 moved toward P1 (`distance 220 -> 169.599976`), entered attack `state=200 anim=200`, damaged P1 (`977 -> 885`), P1 still hit/damaged P2 (`1000 -> 943`), timer stayed in fight phase, and scenario exited cleanly | PASS | Focused CPU baseline fix and P2 hit-resolution follow-up on 2026-06-05. No hardcoded CPU ChangeState, direct damage, specials, character-specific AI, Dragon Mode, store/crafting, sidecar, or `.dragon/*.json` changes. Single Player hitbox-debug display was not part of this proof. |
| Training state boundary | N/A | Static/architecture plus Training live smoke | Extract `TrainingOptions` storage only | `AppState` should aggregate `TrainingState` while training runtime behavior and shared hit/result display messages stay out of TrainingState | `TrainingState.h` now stores `TrainingOptions options`; all `state.trainingOptions` references now use `state.training.options`; `lastHitText` and `lastHitTextTicks` stayed out of `TrainingState` and later moved to `FightMessageState`; user live retest confirmed Training route, F2 labels/statuses, F1, `R`, one normal input, diagonal jump landing, air attack landing, Escape backout, clean exit, CPU baseline behavior, and match-result controller D-pad navigation | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `App.cpp` count changed `14494` -> `14495`; `TrainingState.h` is 11 lines. |
| Fight message state boundary | N/A | Static/architecture | Extract shared fight-message storage only | `AppState` should aggregate `FightMessageState` while hit/damage, round-flow, HUD rendering, verifier scenarios, and runtime behavior stay in `App.cpp` | `FightMessageState.h` now stores `lastHitText` and `lastHitTextTicks`; all direct AppState references now use `state.messages`; verifier snapshots still expose `lastHitText`; `AppState` and `FighterState` stayed in `App.cpp` | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `App.cpp` count stayed `14495`; `FightMessageState.h` is 12 lines. Manual GUI smoke was not rerun in this pass. |
| Fight display state boundary | N/A | Static/architecture | Extract env-shake and combo display storage only | `AppState` should aggregate `FightDisplayState` while env-shake update behavior, combo mutation, camera, global pause, match/round state, hit/damage, rendering, CPU behavior, and runtime systems stay in `App.cpp` | `FightDisplayState.h` now stores env-shake display fields and `comboCounters`; all direct AppState references now use `state.display`; verifier snapshots still expose combo evidence through `state.display.comboCounters`; `AppState` and `FighterState` stayed in `App.cpp` | PASS | Build, `dev_check`, `kfm-baseline`, `evilken-smoke`, `kfm-air-state`, and `cpu-baseline` passed. `App.cpp` count changed `14495` -> `14490`; `FightDisplayState.h` is 19 lines. Manual GUI smoke was not rerun in this pass. |
| Pause menu extraction | Kung Fu Man | Training | Post-extraction Training smoke | Training path and overlays remain unaffected | Title -> Training -> character select -> stage select -> VS -> fight worked; command/input HUD, F1 hitboxes, F2 options, `R`, and Escape backout were observed | PASS | GUI smoke after pause extraction plus user-supplied screenshots on 2026-06-04 confirming command/input HUD, F1 hitboxes, F2 Training Options, stage select backout, movement/combat-visible state, and Training overlay persistence. This verifies unaffected Training render path, not match-pause rendering. |
| Training tools | Kung Fu Man | Training | `R` reset | Fight resets to stage starts | Manual physical keyboard report says reset works without crash | PASS | User-observed manual evidence on 2026-06-04. |
| KFM baseline | Kung Fu Man | Training | Idle | KFM idles in fight view | KFM idle pose/animation visible | PASS | Fight view remained active. |
| KFM physical keyboard | Kung Fu Man | Training | Held left/right | Fighter moves continuously while held, stops on release, and keeps walking animation active | Manual retest after second targeted fix: "ok its all good now" | PASS | Physical keyboard path reaches movement runtime and walk animation now cycles correctly after state `20` starts action `20`/`21` on entry. |
| KFM physical keyboard | Kung Fu Man | Training | Held down | Fighter crouches under held input and returns after release | Manual report says the listed crouch check works | PASS | User-observed manual evidence on 2026-06-04. |
| KFM physical keyboard | Kung Fu Man | Training | Jump | Fighter jumps, lands, and returns grounded | Manual report says the listed jump check works | PASS | User-observed manual evidence on 2026-06-04. |
| KFM physical keyboard | Kung Fu Man | Training | Standing normals | Buttons enter authored normal attack states and recover | Manual report says the listed standing-normal check works | PASS | User-observed manual evidence on 2026-06-04; exact per-button matrix still needs a later detailed pass. |
| KFM physical keyboard | Kung Fu Man | Training | Crouching normals | Down plus attack enters crouching attack behavior | Manual report says the listed crouching-normal check works | PASS | User-observed manual evidence on 2026-06-04; exact per-button matrix still needs a later detailed pass. |
| KFM physical keyboard | Kung Fu Man | Training | Hit/damage | Attack contacts dummy/opponent and produces hit/damage evidence | Manual report says the listed hit/damage check works | PASS | User-observed manual evidence on 2026-06-04. Guard and KO remain separate unverified checks. |
| KFM physical keyboard | Kung Fu Man | Training | Run forward | Forward run/dash behavior works from physical keyboard | Manual report says run forward works | PASS | User-observed manual evidence on 2026-06-04. |
| KFM physical keyboard | Kung Fu Man | Training | Dash backward | Back dash behavior works from physical keyboard | Manual report says dash backward works | PASS | User-observed manual evidence on 2026-06-04. |
| KFM physical keyboard | Kung Fu Man | Training | Post-input-extraction smoke | Held forward/back walk, crouch, jump, one normal, and Esc exit still work | User retested after input boundary extraction and reported working | PASS | Confirms the extracted SDL/gamepad input boundary preserved P1 keyboard behavior. |
| KFM physical keyboard | Kung Fu Man | Training | Hitpause/spark/sound | Hit contact shows hitpause, spark, and sound feedback | User retested after input boundary extraction and reported hitpause/spark/sound working | PASS | Physical keyboard/manual evidence; scripted snapshot still records spark lifecycle as partial because active effects are not exposed at final snapshot. |
| KFM scripted core | Kung Fu Man | Training | `--verify kfm-baseline` controllable idle | KFM reaches state `0` with control | `state=0 anim=0 ctrl=1` | PASS | Internal symbolic input/CMD/CNS runtime path, not physical keyboard. |
| KFM scripted core | Kung Fu Man | Training | Hold right 60 frames | Position changes right | `x_before=-70 x_after=38 delta=108` | PASS | Proves internal held-input path through fighter input and movement runtime. |
| KFM scripted core | Kung Fu Man | Training | Hold left 60 frames | Position changes left | `x_before=38 x_after=-93.999977 delta=-131.999969` | PASS | Proves internal held-input path through fighter input and movement runtime. |
| KFM scripted core | Kung Fu Man | Training | Hold down | Crouch state/animation reached | `state=11 anim=11` | PASS | Internal scripted input path. |
| KFM scripted core | Kung Fu Man | Training | Jump and land | Airborne motion then grounded final state | `y_min=-73.079994 grounded_final=true` | PASS | Internal scripted input path. |
| KFM scripted core | Kung Fu Man | Training | Standing normal | CMD/CNS-driven attack state | `command=y state_before=0 state_after=210 anim_before=0 anim_after=210` | PASS | Does not directly set state or animation. |
| KFM scripted core | Kung Fu Man | Training | Crouching normal | CMD/CNS-driven crouch attack state | `command=y state_before=11 state_after=410 anim_before=11 anim_after=410` | PASS | Does not directly set state or animation. |
| KFM scripted core | Kung Fu Man | Training | Hit contact | Hit/contact event observed | `contact=1 last_hit="P1 hit 210#2 dmg 57 attr S, NA hit 15 spark 1 snd 5,2"` | PASS | Internal scripted path with deterministic spacing. |
| KFM scripted core | Kung Fu Man | Training | Damage | P2 life decreases after hit | `p2_life_before=1000 p2_life_after=943 delta=-57` | PASS | Damage was not directly mutated by the verifier. |
| KFM scripted core | Kung Fu Man | Training | Hitpause/spark/sound | Optional observations available | `hitpause_observed=1 active_effects=0 active_sounds=10` | PARTIAL | Hitpause/sound were observable; spark lifecycle was not numerically exposed at final snapshot. |
| KFM scripted core | Kung Fu Man | Training | Clean scenario exit | Scenario exits with code `0` | `SUMMARY pass=11 partial=1 fail=0 blocked=0` | PASS | Build command used: `build\dragon_mugen.exe --verify kfm-baseline`. |
| Evil Ryu smoke | Evil Ryu | Training | Load/idle/move/normal/special | Character loads and basic actions do not glitch/crash | Not attempted; character selection navigation blocked by unreliable Down/Right automation input | BLOCKED | Static load audit still passes with warnings. |
| Evil Ken visual smoke | Evil Ken | Fight | Fight load | Evil Ken reaches fight presentation | Evil Ken is visible in fight view on Mountainside Temple | PASS | Screenshot-backed visual verification from 2026-06-04. This proves visual reachability, not full compatibility. |
| Evil Ken visual smoke | Evil Ken | Fight | Idle/stance | Evil Ken renders stable stance frames | Evil Ken stance/idle frames are visible | PASS | Screenshot-backed visual verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Movement/repositioning | Character position changes during fight without crashing | Evil Ken appears at different stage positions | PARTIAL | Screenshot-backed visual evidence; exact held-input path still needs deterministic or manual verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Jump/airborne | Character enters airborne visual state | Evil Ken jump/airborne frames are visible | PARTIAL | Screenshot-backed visual evidence; exact input path and landing behavior still need deterministic verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Round intro | Round intro appears before fight | `ROUND 1` overlay appears | PASS | Screenshot-backed visual verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Timer countdown | Timer runs during fight | Timer values shown at multiple counts | PASS | Screenshot-backed visual verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Combo counter | Combo display appears during contact | `4 Rush!` combo counter visible | PARTIAL | Confirms combo UI can appear; damage math and hit event values still need deterministic verification. |
| Evil Ken visual smoke | Evil Ken | Fight | Stage/HUD stability | Stage and HUD continue rendering during fight | Mountainside Temple, lifebars, power bars, names, and timer remain visible | PASS | Screenshot-backed visual verification. |
| Evil Ken scripted smoke | Evil Ken | Single Player | `--verify evilken-smoke` load/idle | Evil Ken loads and reaches stable stance | `state=0 anim=0` | PASS | Internal symbolic input/CMD/CNS runtime path, not physical keyboard. |
| Evil Ken scripted smoke | Evil Ken | Single Player | Movement | Position changes safely | `x_before=-70 x_after=57.250015 delta=127.250015` | PASS | Internal scripted input path. |
| Evil Ken scripted smoke | Evil Ken | Single Player | Jump/airborne | Airborne state observed | `airborne_observed=1` | PASS | Internal scripted input path. |
| Evil Ken scripted smoke | Evil Ken | Single Player | One normal | CMD/CNS-driven attack state | `command=x state_before=0 state_after=206 anim_before=0 anim_after=206` | PASS | Does not prove full Evil Ken compatibility. |
| Evil Ken scripted smoke | Evil Ken | Single Player | Round/timer stability | Timer/round systems keep running | `match_phase=1 timer_ticks=5650` | PASS | Internal scripted smoke. |
| Evil Ken scripted smoke | Evil Ken | Single Player | Combo or hit evidence | Contact evidence if available | `combo_hits=1 last_hit="P1 hit 206#2 dmg 0 attr S, NA hit 12 spark 0 snd 6,1"` | PASS | Damage was `0` for this smoke hit, so full damage compatibility remains unproven. |
| Evil Ken scripted smoke | Evil Ken | Single Player | Clean scenario exit | Scenario exits with code `0` | `SUMMARY pass=7 partial=0 fail=0 blocked=0` | PASS | Build command used: `build\dragon_mugen.exe --verify evilken-smoke`. |
| P2 keyboard | Kung Fu Man | VS or Training P2 control | Movement and attack buttons | P2 responds where local P2 is expected | User tested P2 keyboard in a real P2 mode and reported working | PASS | Manual physical keyboard evidence after input boundary extraction. Detailed per-button matrix can be expanded later. |
| Controller input | Evil Ryu | Options/VS Mode | PlayStation controller assignment and labels | Connected controller is detected and PlayStation labels render when selected | User-supplied evidence shows `PAD LABELS` set to PlayStation, P1 gamepad assigned, console output `Gamepad connected: DualSense Edge Wireless Controller (PlayStation)`, and VS fight HUD with `Sq/Tri/L1 Cross/Cir/R1 Start pause` | PASS | Manual user evidence on 2026-06-04. A stale `Failed to open gamepad 4` warning appeared before the successful DualSense connection and did not block play. |
| Controller input | Evil Ryu | VS Mode | Controller fight smoke and face-button attacks | Controller input reaches fight runtime and can produce combat events | User reported controller works; screenshot shows Evil Ryu vs Player 2 in VS fight; console output shows repeated P1 and P2 hit logs with damage/spark/sound fields | PASS | This is a live controller smoke pass, not a complete per-button certification. |
| Controller input | Evil Ryu | VS Mode | Start/back/cancel actions | Controller Start/back/cancel should perform current mapped pause/cancel actions | HUD label shows `Start pause`, but Start/back/cancel actions were not separately recorded in evidence | NOT TESTED | Keep separate from controller movement/attack smoke. |
| P2 local fight | Evil Ryu | VS Mode | Player 2 fight and hit events | Local Player 2 fight loads and both sides can produce hit evidence | Screenshot shows `Evil Ryu vs Player 2`; console logs show both `P1 hit ...` and `P2 hit ...` events | PASS | Manual user evidence on 2026-06-04 after Options extraction. |
