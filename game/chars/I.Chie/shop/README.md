# I.Chie Shop Assets

Shop presentation assets for I.Chie live here so the shopkeeper presentation is owned by the `I.Chie` character folder.

- `shopkeeper_pose.png`: current behind-counter shopkeeper pose.

The legacy root-level `I.Chie_shopkeeper_pose.png` remains as compatibility fallback.
