# Story UI Assets

Optional story-mode presentation assets live here.

- `wave_clear_arrow.png`: replaces the code-drawn `CLEAR >>>` wave-clear cue.

Use a transparent PNG. The engine preserves the image aspect ratio, pulses its alpha,
and falls back to the built-in `CLEAR >>>` cue when the file is absent or fails to load.
